<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/base_runner (snapshot).py">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/base_runner (snapshot).py" />
              <option name="originalContent" value="&#10;import time&#10;import wandb&#10;import os&#10;import numpy as np&#10;from itertools import chain&#10;import torch&#10;from tensorboardX import SummaryWriter&#10;&#10;from onpolicy.utils.separated_buffer import SeparatedReplayBuffer&#10;from onpolicy.utils.util import update_linear_schedule&#10;&#10;def _t2n(x):&#10;    return x.detach().cpu().numpy()&#10;&#10;class Runner(object):&#10;    def __init__(self, config):&#10;&#10;        self.all_args = config['all_args']&#10;        self.envs = config['envs']&#10;        self.eval_envs = config['eval_envs']&#10;        self.device = config['device']&#10;        self.num_agents = config['num_agents']&#10;&#10;        # parameters&#10;        self.env_name = self.all_args.env_name&#10;        self.algorithm_name = self.all_args.algorithm_name&#10;        self.experiment_name = self.all_args.experiment_name&#10;        self.use_centralized_V = self.all_args.use_centralized_V&#10;        self.use_obs_instead_of_state = self.all_args.use_obs_instead_of_state&#10;        self.num_env_steps = self.all_args.num_env_steps&#10;        self.episode_length = self.all_args.episode_length&#10;        self.n_rollout_threads = self.all_args.n_rollout_threads&#10;        self.n_eval_rollout_threads = self.all_args.n_eval_rollout_threads&#10;        self.use_linear_lr_decay = self.all_args.use_linear_lr_decay&#10;        self.hidden_size = self.all_args.hidden_size&#10;        self.use_wandb = self.all_args.use_wandb&#10;        self.use_render = self.all_args.use_render&#10;        self.recurrent_N = self.all_args.recurrent_N&#10;&#10;        # interval&#10;        self.save_interval = self.all_args.save_interval&#10;        self.use_eval = self.all_args.use_eval&#10;        self.eval_interval = self.all_args.eval_interval&#10;        self.log_interval = self.all_args.log_interval&#10;&#10;        # dir&#10;        self.model_dir = self.all_args.model_dir&#10;&#10;        if self.use_render:&#10;            import imageio&#10;            self.run_dir = config[&quot;run_dir&quot;]&#10;            self.gif_dir = str(self.run_dir / 'gifs')&#10;            if not os.path.exists(self.gif_dir):&#10;                os.makedirs(self.gif_dir)&#10;        else:&#10;            if self.use_wandb:&#10;                self.save_dir = str(wandb.run.dir)&#10;            else:&#10;                self.run_dir = config[&quot;run_dir&quot;]&#10;                self.log_dir = str(self.run_dir / 'logs')&#10;                if not os.path.exists(self.log_dir):&#10;                    os.makedirs(self.log_dir)&#10;                self.writter = SummaryWriter(self.log_dir)&#10;                self.save_dir = str(self.run_dir / 'models')&#10;                if not os.path.exists(self.save_dir):&#10;                    os.makedirs(self.save_dir)&#10;&#10;&#10;        if self.all_args.algorithm_name == &quot;happo&quot;:&#10;            from onpolicy.algorithms.happo.happo_trainer import HAPPO as TrainAlgo&#10;            from onpolicy.algorithms.happo.policy import HAPPO_Policy as Policy&#10;        elif self.all_args.algorithm_name == &quot;hatrpo&quot;:&#10;            from onpolicy.algorithms.hatrpo.hatrpo_trainer import HATRPO as TrainAlgo&#10;            from onpolicy.algorithms.hatrpo.policy import HATRPO_Policy as Policy&#10;        else:&#10;            from onpolicy.algorithms.r_mappo.r_mappo import R_MAPPO as TrainAlgo&#10;            from onpolicy.algorithms.r_mappo.algorithm.rMAPPOPolicy import R_MAPPOPolicy as Policy&#10;&#10;        # VA-GGP: optionally use specialised policy/trainer for adversary agents&#10;        self._use_vaggp = getattr(self.all_args, 'use_vaggp', False)&#10;        self._num_adversaries = getattr(self.all_args, 'num_adversaries', 0)&#10;        VAGGP_TrainAlgo = None&#10;        VAGGP_PolicyCls = None&#10;        if self._use_vaggp:&#10;            from onpolicy.algorithms.r_mappo.vaggp_trainer import VAGGP_Trainer as VAGGP_TrainAlgo&#10;            from onpolicy.algorithms.r_mappo.algorithm.vaggp_policy import VAGGP_Policy as VAGGP_PolicyCls&#10;            print(&quot;[VA-GGP] Enabled for adversary agents 0..{}.&quot;.format(self._num_adversaries - 1))&#10;&#10;        print(&quot;share_observation_space: &quot;, self.envs.share_observation_space)&#10;        print(&quot;observation_space: &quot;, self.envs.observation_space)&#10;        print(&quot;action_space: &quot;, self.envs.action_space)&#10;&#10;        self.policy = []&#10;        for agent_id in range(self.num_agents):&#10;            share_observation_space = self.envs.share_observation_space[agent_id] if self.use_centralized_V else self.envs.observation_space[agent_id]&#10;            # VA-GGP policy for adversary agents, standard policy for others&#10;            if self._use_vaggp and agent_id &lt; self._num_adversaries:&#10;                enable_guider = (agent_id &gt; 0)  # agent 0 (leader) has no prev agents for guider context&#10;                po = VAGGP_PolicyCls(self.all_args,&#10;                            self.envs.observation_space[agent_id],&#10;                            share_observation_space,&#10;                            self.envs.action_space[agent_id],&#10;                            device=self.device,&#10;                            enable_guider=enable_guider,&#10;                            num_adversaries=self._num_adversaries,&#10;                            adv_agent_id=agent_id)&#10;            else:&#10;                po = Policy(self.all_args,&#10;                            self.envs.observation_space[agent_id],&#10;                            share_observation_space,&#10;                            self.envs.action_space[agent_id],&#10;                            device = self.device)&#10;            self.policy.append(po)&#10;&#10;        self.trainer = []&#10;        self.buffer = []&#10;        for agent_id in range(self.num_agents):&#10;            # VA-GGP trainer for adversary agents, standard trainer for others&#10;            if self._use_vaggp and agent_id &lt; self._num_adversaries:&#10;                tr = VAGGP_TrainAlgo(self.all_args, self.policy[agent_id], device=self.device)&#10;            else:&#10;                tr = TrainAlgo(self.all_args, self.policy[agent_id], device = self.device)&#10;            # buffer&#10;            share_observation_space = self.envs.share_observation_space[agent_id] if self.use_centralized_V else self.envs.observation_space[agent_id]&#10;            bu = SeparatedReplayBuffer(self.all_args,&#10;                                       self.envs.observation_space[agent_id],&#10;                                       share_observation_space,&#10;                                       self.envs.action_space[agent_id])&#10;            self.buffer.append(bu)&#10;            self.trainer.append(tr)&#10;&#10;        if self.model_dir is not None:&#10;            self.restore()&#10;            &#10;    def run(self):&#10;        raise NotImplementedError&#10;&#10;    def warmup(self):&#10;        raise NotImplementedError&#10;&#10;    def collect(self, step):&#10;        raise NotImplementedError&#10;&#10;    def insert(self, data):&#10;        raise NotImplementedError&#10;    &#10;    @torch.no_grad()&#10;    def compute(self):&#10;        for agent_id in range(self.num_agents):&#10;            self.trainer[agent_id].prep_rollout()&#10;            next_value = self.trainer[agent_id].policy.get_values(self.buffer[agent_id].share_obs[-1], &#10;                                                                self.buffer[agent_id].rnn_states_critic[-1],&#10;                                                                self.buffer[agent_id].masks[-1])&#10;            next_value = _t2n(next_value)&#10;            self.buffer[agent_id].compute_returns(next_value, self.trainer[agent_id].value_normalizer)&#10;&#10;    def train(self):&#10;        train_infos = []&#10;        # random update order&#10;&#10;        action_dim=self.buffer[0].actions.shape[-1]&#10;        factor = np.ones((self.episode_length, self.n_rollout_threads, 1), dtype=np.float32)&#10;&#10;        # VA-GGP: collect adversary buffers for autoregressive context&#10;        all_adv_buffers = None&#10;        if self._use_vaggp and self._num_adversaries &gt; 0:&#10;            all_adv_buffers = [self.buffer[i] for i in range(self._num_adversaries)]&#10;&#10;        for agent_id in torch.randperm(self.num_agents):&#10;            self.trainer[agent_id].prep_training()&#10;            self.buffer[agent_id].update_factor(factor)&#10;            available_actions = None if self.buffer[agent_id].available_actions is None \&#10;                else self.buffer[agent_id].available_actions[:-1].reshape(-1, *self.buffer[agent_id].available_actions.shape[2:])&#10;            &#10;            if self.all_args.algorithm_name == &quot;hatrpo&quot;:&#10;                old_actions_logprob, _, _, _, _ =self.trainer[agent_id].policy.actor.evaluate_actions(self.buffer[agent_id].obs[:-1].reshape(-1, *self.buffer[agent_id].obs.shape[2:]),&#10;                                                            self.buffer[agent_id].rnn_states[0:1].reshape(-1, *self.buffer[agent_id].rnn_states.shape[2:]),&#10;                                                            self.buffer[agent_id].actions.reshape(-1, *self.buffer[agent_id].actions.shape[2:]),&#10;                                                            self.buffer[agent_id].masks[:-1].reshape(-1, *self.buffer[agent_id].masks.shape[2:]),&#10;                                                            available_actions,&#10;                                                            self.buffer[agent_id].active_masks[:-1].reshape(-1, *self.buffer[agent_id].active_masks.shape[2:]))&#10;            else:&#10;                eval_result = self.trainer[agent_id].policy.actor.evaluate_actions(self.buffer[agent_id].obs[:-1].reshape(-1, *self.buffer[agent_id].obs.shape[2:]),&#10;                                                            self.buffer[agent_id].rnn_states[0:1].reshape(-1, *self.buffer[agent_id].rnn_states.shape[2:]),&#10;                                                            self.buffer[agent_id].actions.reshape(-1, *self.buffer[agent_id].actions.shape[2:]),&#10;                                                            self.buffer[agent_id].masks[:-1].reshape(-1, *self.buffer[agent_id].masks.shape[2:]),&#10;                                                            available_actions,&#10;                                                            self.buffer[agent_id].active_masks[:-1].reshape(-1, *self.buffer[agent_id].active_masks.shape[2:]))&#10;                # VA-GGP evaluate_actions returns 3 values (lp, ent, info); base returns 2 (lp, ent)&#10;                old_actions_logprob = eval_result[0]&#10;&#10;            # VA-GGP training with adversary buffers for autoregressive context&#10;            if self._use_vaggp and agent_id &lt; self._num_adversaries:&#10;                train_info = self.trainer[agent_id].train(&#10;                    self.buffer[agent_id],&#10;                    all_adv_buffers=all_adv_buffers,&#10;                    adv_order_idx=int(agent_id))&#10;            else:&#10;                train_info = self.trainer[agent_id].train(self.buffer[agent_id])&#10;&#10;            if self.all_args.algorithm_name == &quot;hatrpo&quot;:&#10;                new_actions_logprob, _, _, _, _ =self.trainer[agent_id].policy.actor.evaluate_actions(self.buffer[agent_id].obs[:-1].reshape(-1, *self.buffer[agent_id].obs.shape[2:]),&#10;                                                            self.buffer[agent_id].rnn_states[0:1].reshape(-1, *self.buffer[agent_id].rnn_states.shape[2:]),&#10;                                                            self.buffer[agent_id].actions.reshape(-1, *self.buffer[agent_id].actions.shape[2:]),&#10;                                                            self.buffer[agent_id].masks[:-1].reshape(-1, *self.buffer[agent_id].masks.shape[2:]),&#10;                                                            available_actions,&#10;                                                            self.buffer[agent_id].active_masks[:-1].reshape(-1, *self.buffer[agent_id].active_masks.shape[2:]))&#10;            else:&#10;                new_eval_result = self.trainer[agent_id].policy.actor.evaluate_actions(self.buffer[agent_id].obs[:-1].reshape(-1, *self.buffer[agent_id].obs.shape[2:]),&#10;                                                            self.buffer[agent_id].rnn_states[0:1].reshape(-1, *self.buffer[agent_id].rnn_states.shape[2:]),&#10;                                                            self.buffer[agent_id].actions.reshape(-1, *self.buffer[agent_id].actions.shape[2:]),&#10;                                                            self.buffer[agent_id].masks[:-1].reshape(-1, *self.buffer[agent_id].masks.shape[2:]),&#10;                                                            available_actions,&#10;                                                            self.buffer[agent_id].active_masks[:-1].reshape(-1, *self.buffer[agent_id].active_masks.shape[2:]))&#10;                new_actions_logprob = new_eval_result[0]&#10;&#10;            factor = factor*_t2n(torch.prod(torch.exp(new_actions_logprob-old_actions_logprob),dim=-1).reshape(self.episode_length,self.n_rollout_threads,1))&#10;            train_infos.append(train_info)      &#10;            self.buffer[agent_id].after_update()&#10;&#10;        return train_infos&#10;&#10;    def save(self):&#10;        for agent_id in range(self.num_agents):&#10;            policy_actor = self.trainer[agent_id].policy.actor&#10;            torch.save(policy_actor.state_dict(), str(self.save_dir) + &quot;/actor_agent&quot; + str(agent_id) + &quot;.pt&quot;)&#10;            policy_critic = self.trainer[agent_id].policy.critic&#10;            torch.save(policy_critic.state_dict(), str(self.save_dir) + &quot;/critic_agent&quot; + str(agent_id) + &quot;.pt&quot;)&#10;            if self.trainer[agent_id]._use_valuenorm:&#10;                policy_vnrom = self.trainer[agent_id].value_normalizer&#10;                torch.save(policy_vnrom.state_dict(), str(self.save_dir) + &quot;/vnrom_agent&quot; + str(agent_id) + &quot;.pt&quot;)&#10;            # VA-GGP: save guider weights&#10;            if self._use_vaggp and agent_id &lt; self._num_adversaries:&#10;                guider = self.policy[agent_id].guider&#10;                if guider is not None:&#10;                    torch.save(guider.state_dict(), str(self.save_dir) + &quot;/guider_agent&quot; + str(agent_id) + &quot;.pt&quot;)&#10;&#10;    def restore(self):&#10;        for agent_id in range(self.num_agents):&#10;            policy_actor_state_dict = torch.load(str(self.model_dir) + '/actor_agent' + str(agent_id) + '.pt')&#10;            self.policy[agent_id].actor.load_state_dict(policy_actor_state_dict)&#10;            policy_critic_state_dict = torch.load(str(self.model_dir) + '/critic_agent' + str(agent_id) + '.pt')&#10;            self.policy[agent_id].critic.load_state_dict(policy_critic_state_dict)&#10;            if self.trainer[agent_id]._use_valuenorm:&#10;                vnrom_path = str(self.model_dir) + '/vnrom_agent' + str(agent_id) + '.pt'&#10;                if os.path.exists(vnrom_path):&#10;                    policy_vnrom_state_dict = torch.load(vnrom_path)&#10;                    self.trainer[agent_id].value_normalizer.load_state_dict(policy_vnrom_state_dict)&#10;                else:&#10;                    print(&quot;[restore] WARNING: valuenorm is enabled but {} not found, skipping.&quot;.format(vnrom_path))&#10;            # VA-GGP: restore guider weights&#10;            if self._use_vaggp and agent_id &lt; self._num_adversaries:&#10;                guider = self.policy[agent_id].guider&#10;                if guider is not None:&#10;                    guider_path = str(self.model_dir) + '/guider_agent' + str(agent_id) + '.pt'&#10;                    if os.path.exists(guider_path):&#10;                        guider.load_state_dict(torch.load(guider_path))&#10;                    else:&#10;                        print(&quot;[restore] WARNING: guider checkpoint {} not found, skipping.&quot;.format(guider_path))&#10;&#10;    def log_train(self, train_infos, total_num_steps): &#10;        for agent_id in range(self.num_agents):&#10;            for k, v in train_infos[agent_id].items():&#10;                agent_k = &quot;agent%i/&quot; % agent_id + k&#10;                if self.use_wandb:&#10;                    wandb.log({agent_k: v}, step=total_num_steps)&#10;                else:&#10;                    self.writter.add_scalars(agent_k, {agent_k: v}, total_num_steps)&#10;&#10;    def log_env(self, env_infos, total_num_steps):&#10;        for k, v in env_infos.items():&#10;            if len(v) &gt; 0:&#10;                if self.use_wandb:&#10;                    wandb.log({k: np.mean(v)}, step=total_num_steps)&#10;                else:&#10;                    self.writter.add_scalars(k, {k: np.mean(v)}, total_num_steps)&#10;" />
              <option name="updatedContent" value="&#10;import time&#10;import wandb&#10;import os&#10;import numpy as np&#10;from itertools import chain&#10;import torch&#10;from tensorboardX import SummaryWriter&#10;&#10;from onpolicy.utils.separated_buffer import SeparatedReplayBuffer&#10;from onpolicy.utils.util import update_linear_schedule&#10;&#10;def _t2n(x):&#10;    return x.detach().cpu().numpy()&#10;&#10;class Runner(object):&#10;    def __init__(self, config):&#10;&#10;        self.all_args = config['all_args']&#10;        self.envs = config['envs']&#10;        self.eval_envs = config['eval_envs']&#10;        self.device = config['device']&#10;        self.num_agents = config['num_agents']&#10;&#10;        # parameters&#10;        self.env_name = self.all_args.env_name&#10;        self.algorithm_name = self.all_args.algorithm_name&#10;        self.experiment_name = self.all_args.experiment_name&#10;        self.use_centralized_V = self.all_args.use_centralized_V&#10;        self.use_obs_instead_of_state = self.all_args.use_obs_instead_of_state&#10;        self.num_env_steps = self.all_args.num_env_steps&#10;        self.episode_length = self.all_args.episode_length&#10;        self.n_rollout_threads = self.all_args.n_rollout_threads&#10;        self.n_eval_rollout_threads = self.all_args.n_eval_rollout_threads&#10;        self.use_linear_lr_decay = self.all_args.use_linear_lr_decay&#10;        self.hidden_size = self.all_args.hidden_size&#10;        self.use_wandb = self.all_args.use_wandb&#10;        self.use_render = self.all_args.use_render&#10;        self.recurrent_N = self.all_args.recurrent_N&#10;&#10;        # interval&#10;        self.save_interval = self.all_args.save_interval&#10;        self.use_eval = self.all_args.use_eval&#10;        self.eval_interval = self.all_args.eval_interval&#10;        self.log_interval = self.all_args.log_interval&#10;&#10;        # dir&#10;        self.model_dir = self.all_args.model_dir&#10;&#10;        if self.use_render:&#10;            import imageio&#10;            self.run_dir = config[&quot;run_dir&quot;]&#10;            self.gif_dir = str(self.run_dir / 'gifs')&#10;            if not os.path.exists(self.gif_dir):&#10;                os.makedirs(self.gif_dir)&#10;        else:&#10;            if self.use_wandb:&#10;                self.save_dir = str(wandb.run.dir)&#10;            else:&#10;                self.run_dir = config[&quot;run_dir&quot;]&#10;                self.log_dir = str(self.run_dir / 'logs')&#10;                if not os.path.exists(self.log_dir):&#10;                    os.makedirs(self.log_dir)&#10;                self.writter = SummaryWriter(self.log_dir)&#10;                self.save_dir = str(self.run_dir / 'models')&#10;                if not os.path.exists(self.save_dir):&#10;                    os.makedirs(self.save_dir)&#10;&#10;&#10;        if self.all_args.algorithm_name == &quot;happo&quot;:&#10;            from onpolicy.algorithms.happo.happo_trainer import HAPPO as TrainAlgo&#10;            from onpolicy.algorithms.happo.policy import HAPPO_Policy as Policy&#10;        elif self.all_args.algorithm_name == &quot;hatrpo&quot;:&#10;            from onpolicy.algorithms.hatrpo.hatrpo_trainer import HATRPO as TrainAlgo&#10;            from onpolicy.algorithms.hatrpo.policy import HATRPO_Policy as Policy&#10;        else:&#10;            from onpolicy.algorithms.r_mappo.r_mappo import R_MAPPO as TrainAlgo&#10;            from onpolicy.algorithms.r_mappo.algorithm.rMAPPOPolicy import R_MAPPOPolicy as Policy&#10;&#10;        # VA-GGP: optionally use specialised policy/trainer for adversary agents&#10;        self._use_vaggp = getattr(self.all_args, 'use_vaggp', False)&#10;        self._num_adversaries = getattr(self.all_args, 'num_adversaries', 0)&#10;        VAGGP_TrainAlgo = None&#10;        VAGGP_PolicyCls = None&#10;        if self._use_vaggp:&#10;            from onpolicy.algorithms.r_mappo.vaggp_trainer import VAGGP_Trainer as VAGGP_TrainAlgo&#10;            from onpolicy.algorithms.r_mappo.algorithm.vaggp_policy import VAGGP_Policy as VAGGP_PolicyCls&#10;            print(&quot;[VA-GGP] Enabled for adversary agents 0..{}.&quot;.format(self._num_adversaries - 1))&#10;&#10;        print(&quot;share_observation_space: &quot;, self.envs.share_observation_space)&#10;        print(&quot;observation_space: &quot;, self.envs.observation_space)&#10;        print(&quot;action_space: &quot;, self.envs.action_space)&#10;&#10;        self.policy = []&#10;        for agent_id in range(self.num_agents):&#10;            share_observation_space = self.envs.share_observation_space[agent_id] if self.use_centralized_V else self.envs.observation_space[agent_id]&#10;            # VA-GGP policy for adversary agents, standard policy for others&#10;            if self._use_vaggp and agent_id &lt; self._num_adversaries:&#10;                enable_guider = (agent_id &gt; 0)  # agent 0 has no prev agents for AR guider context&#10;                po = VAGGP_PolicyCls(self.all_args,&#10;                            self.envs.observation_space[agent_id],&#10;                            share_observation_space,&#10;                            self.envs.action_space[agent_id],&#10;                            device=self.device,&#10;                            enable_guider=enable_guider,&#10;                            num_adversaries=self._num_adversaries,&#10;                            adv_agent_id=agent_id)&#10;            else:&#10;                po = Policy(self.all_args,&#10;                            self.envs.observation_space[agent_id],&#10;                            share_observation_space,&#10;                            self.envs.action_space[agent_id],&#10;                            device = self.device)&#10;            self.policy.append(po)&#10;&#10;        self.trainer = []&#10;        self.buffer = []&#10;        for agent_id in range(self.num_agents):&#10;            # VA-GGP trainer for adversary agents, standard trainer for others&#10;            if self._use_vaggp and agent_id &lt; self._num_adversaries:&#10;                tr = VAGGP_TrainAlgo(self.all_args, self.policy[agent_id], device=self.device)&#10;            else:&#10;                tr = TrainAlgo(self.all_args, self.policy[agent_id], device = self.device)&#10;            # buffer&#10;            share_observation_space = self.envs.share_observation_space[agent_id] if self.use_centralized_V else self.envs.observation_space[agent_id]&#10;            bu = SeparatedReplayBuffer(self.all_args,&#10;                                       self.envs.observation_space[agent_id],&#10;                                       share_observation_space,&#10;                                       self.envs.action_space[agent_id])&#10;            self.buffer.append(bu)&#10;            self.trainer.append(tr)&#10;&#10;        if self.model_dir is not None:&#10;            self.restore()&#10;            &#10;    def run(self):&#10;        raise NotImplementedError&#10;&#10;    def warmup(self):&#10;        raise NotImplementedError&#10;&#10;    def collect(self, step):&#10;        raise NotImplementedError&#10;&#10;    def insert(self, data):&#10;        raise NotImplementedError&#10;    &#10;    @torch.no_grad()&#10;    def compute(self):&#10;        for agent_id in range(self.num_agents):&#10;            self.trainer[agent_id].prep_rollout()&#10;            next_value = self.trainer[agent_id].policy.get_values(self.buffer[agent_id].share_obs[-1], &#10;                                                                self.buffer[agent_id].rnn_states_critic[-1],&#10;                                                                self.buffer[agent_id].masks[-1])&#10;            next_value = _t2n(next_value)&#10;            self.buffer[agent_id].compute_returns(next_value, self.trainer[agent_id].value_normalizer)&#10;&#10;    def train(self):&#10;        train_infos = []&#10;        # random update order&#10;&#10;        action_dim=self.buffer[0].actions.shape[-1]&#10;        factor = np.ones((self.episode_length, self.n_rollout_threads, 1), dtype=np.float32)&#10;&#10;        # VA-GGP: collect adversary buffers for autoregressive context&#10;        all_adv_buffers = None&#10;        if self._use_vaggp and self._num_adversaries &gt; 0:&#10;            all_adv_buffers = [self.buffer[i] for i in range(self._num_adversaries)]&#10;&#10;        for agent_id in torch.randperm(self.num_agents):&#10;            self.trainer[agent_id].prep_training()&#10;            self.buffer[agent_id].update_factor(factor)&#10;            available_actions = None if self.buffer[agent_id].available_actions is None \&#10;                else self.buffer[agent_id].available_actions[:-1].reshape(-1, *self.buffer[agent_id].available_actions.shape[2:])&#10;            &#10;            if self.all_args.algorithm_name == &quot;hatrpo&quot;:&#10;                old_actions_logprob, _, _, _, _ =self.trainer[agent_id].policy.actor.evaluate_actions(self.buffer[agent_id].obs[:-1].reshape(-1, *self.buffer[agent_id].obs.shape[2:]),&#10;                                                            self.buffer[agent_id].rnn_states[0:1].reshape(-1, *self.buffer[agent_id].rnn_states.shape[2:]),&#10;                                                            self.buffer[agent_id].actions.reshape(-1, *self.buffer[agent_id].actions.shape[2:]),&#10;                                                            self.buffer[agent_id].masks[:-1].reshape(-1, *self.buffer[agent_id].masks.shape[2:]),&#10;                                                            available_actions,&#10;                                                            self.buffer[agent_id].active_masks[:-1].reshape(-1, *self.buffer[agent_id].active_masks.shape[2:]))&#10;            else:&#10;                eval_result = self.trainer[agent_id].policy.actor.evaluate_actions(self.buffer[agent_id].obs[:-1].reshape(-1, *self.buffer[agent_id].obs.shape[2:]),&#10;                                                            self.buffer[agent_id].rnn_states[0:1].reshape(-1, *self.buffer[agent_id].rnn_states.shape[2:]),&#10;                                                            self.buffer[agent_id].actions.reshape(-1, *self.buffer[agent_id].actions.shape[2:]),&#10;                                                            self.buffer[agent_id].masks[:-1].reshape(-1, *self.buffer[agent_id].masks.shape[2:]),&#10;                                                            available_actions,&#10;                                                            self.buffer[agent_id].active_masks[:-1].reshape(-1, *self.buffer[agent_id].active_masks.shape[2:]))&#10;                # VA-GGP evaluate_actions returns 3 values (lp, ent, info); base returns 2 (lp, ent)&#10;                old_actions_logprob = eval_result[0]&#10;&#10;            # VA-GGP training with adversary buffers for autoregressive context&#10;            if self._use_vaggp and agent_id &lt; self._num_adversaries:&#10;                train_info = self.trainer[agent_id].train(&#10;                    self.buffer[agent_id],&#10;                    all_adv_buffers=all_adv_buffers,&#10;                    adv_order_idx=int(agent_id))&#10;            else:&#10;                train_info = self.trainer[agent_id].train(self.buffer[agent_id])&#10;&#10;            if self.all_args.algorithm_name == &quot;hatrpo&quot;:&#10;                new_actions_logprob, _, _, _, _ =self.trainer[agent_id].policy.actor.evaluate_actions(self.buffer[agent_id].obs[:-1].reshape(-1, *self.buffer[agent_id].obs.shape[2:]),&#10;                                                            self.buffer[agent_id].rnn_states[0:1].reshape(-1, *self.buffer[agent_id].rnn_states.shape[2:]),&#10;                                                            self.buffer[agent_id].actions.reshape(-1, *self.buffer[agent_id].actions.shape[2:]),&#10;                                                            self.buffer[agent_id].masks[:-1].reshape(-1, *self.buffer[agent_id].masks.shape[2:]),&#10;                                                            available_actions,&#10;                                                            self.buffer[agent_id].active_masks[:-1].reshape(-1, *self.buffer[agent_id].active_masks.shape[2:]))&#10;            else:&#10;                new_eval_result = self.trainer[agent_id].policy.actor.evaluate_actions(self.buffer[agent_id].obs[:-1].reshape(-1, *self.buffer[agent_id].obs.shape[2:]),&#10;                                                            self.buffer[agent_id].rnn_states[0:1].reshape(-1, *self.buffer[agent_id].rnn_states.shape[2:]),&#10;                                                            self.buffer[agent_id].actions.reshape(-1, *self.buffer[agent_id].actions.shape[2:]),&#10;                                                            self.buffer[agent_id].masks[:-1].reshape(-1, *self.buffer[agent_id].masks.shape[2:]),&#10;                                                            available_actions,&#10;                                                            self.buffer[agent_id].active_masks[:-1].reshape(-1, *self.buffer[agent_id].active_masks.shape[2:]))&#10;                new_actions_logprob = new_eval_result[0]&#10;&#10;            factor = factor*_t2n(torch.prod(torch.exp(new_actions_logprob-old_actions_logprob),dim=-1).reshape(self.episode_length,self.n_rollout_threads,1))&#10;            train_infos.append(train_info)      &#10;            self.buffer[agent_id].after_update()&#10;&#10;        return train_infos&#10;&#10;    def save(self):&#10;        for agent_id in range(self.num_agents):&#10;            policy_actor = self.trainer[agent_id].policy.actor&#10;            torch.save(policy_actor.state_dict(), str(self.save_dir) + &quot;/actor_agent&quot; + str(agent_id) + &quot;.pt&quot;)&#10;            policy_critic = self.trainer[agent_id].policy.critic&#10;            torch.save(policy_critic.state_dict(), str(self.save_dir) + &quot;/critic_agent&quot; + str(agent_id) + &quot;.pt&quot;)&#10;            if self.trainer[agent_id]._use_valuenorm:&#10;                policy_vnrom = self.trainer[agent_id].value_normalizer&#10;                torch.save(policy_vnrom.state_dict(), str(self.save_dir) + &quot;/vnrom_agent&quot; + str(agent_id) + &quot;.pt&quot;)&#10;            # VA-GGP: save guider weights&#10;            if self._use_vaggp and agent_id &lt; self._num_adversaries:&#10;                guider = self.policy[agent_id].guider&#10;                if guider is not None:&#10;                    torch.save(guider.state_dict(), str(self.save_dir) + &quot;/guider_agent&quot; + str(agent_id) + &quot;.pt&quot;)&#10;&#10;    def restore(self):&#10;        for agent_id in range(self.num_agents):&#10;            policy_actor_state_dict = torch.load(str(self.model_dir) + '/actor_agent' + str(agent_id) + '.pt')&#10;            self.policy[agent_id].actor.load_state_dict(policy_actor_state_dict)&#10;            policy_critic_state_dict = torch.load(str(self.model_dir) + '/critic_agent' + str(agent_id) + '.pt')&#10;            self.policy[agent_id].critic.load_state_dict(policy_critic_state_dict)&#10;            if self.trainer[agent_id]._use_valuenorm:&#10;                vnrom_path = str(self.model_dir) + '/vnrom_agent' + str(agent_id) + '.pt'&#10;                if os.path.exists(vnrom_path):&#10;                    policy_vnrom_state_dict = torch.load(vnrom_path)&#10;                    self.trainer[agent_id].value_normalizer.load_state_dict(policy_vnrom_state_dict)&#10;                else:&#10;                    print(&quot;[restore] WARNING: valuenorm is enabled but {} not found, skipping.&quot;.format(vnrom_path))&#10;            # VA-GGP: restore guider weights&#10;            if self._use_vaggp and agent_id &lt; self._num_adversaries:&#10;                guider = self.policy[agent_id].guider&#10;                if guider is not None:&#10;                    guider_path = str(self.model_dir) + '/guider_agent' + str(agent_id) + '.pt'&#10;                    if os.path.exists(guider_path):&#10;                        guider.load_state_dict(torch.load(guider_path))&#10;                    else:&#10;                        print(&quot;[restore] WARNING: guider checkpoint {} not found, skipping.&quot;.format(guider_path))&#10;&#10;    def log_train(self, train_infos, total_num_steps): &#10;        for agent_id in range(self.num_agents):&#10;            for k, v in train_infos[agent_id].items():&#10;                agent_k = &quot;agent%i/&quot; % agent_id + k&#10;                if self.use_wandb:&#10;                    wandb.log({agent_k: v}, step=total_num_steps)&#10;                else:&#10;                    self.writter.add_scalars(agent_k, {agent_k: v}, total_num_steps)&#10;&#10;    def log_env(self, env_infos, total_num_steps):&#10;        for k, v in env_infos.items():&#10;            if len(v) &gt; 0:&#10;                if self.use_wandb:&#10;                    wandb.log({k: np.mean(v)}, step=total_num_steps)&#10;                else:&#10;                    self.writter.add_scalars(k, {k: np.mean(v)}, total_num_steps)&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="/simple_world_comm (snapshot).py">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/simple_world_comm (snapshot).py" />
              <option name="originalContent" value="import numpy as np&#10;from onpolicy.envs.mpe.core import World, Agent, Landmark&#10;from onpolicy.envs.mpe.scenario import BaseScenario&#10;&#10;class Scenario(BaseScenario):&#10;    def make_world(self,args):&#10;        world = World()&#10;        # set any world properties first&#10;        world.dim_c = 4&#10;        #world.damping = 1&#10;        num_good_agents = args.num_good_agents#2&#10;        num_adversaries = args.num_adversaries#4&#10;        num_agents = num_adversaries + num_good_agents&#10;        num_landmarks = args.num_landmarks#1&#10;        num_food = 2&#10;        num_forests = 2&#10;        # add agents&#10;        world.agents = [Agent() for i in range(num_agents)]&#10;        for i, agent in enumerate(world.agents):&#10;            agent.name = 'agent %d' % i&#10;            agent.collide = True&#10;            agent.leader = False          # no leader — all adversaries are equal&#10;            agent.silent = True           # no comm action — all Discrete(5)&#10;            agent.adversary = True if i &lt; num_adversaries else False&#10;            agent.size = 0.075 if agent.adversary else 0.045&#10;            agent.accel = 3.0 if agent.adversary else 4.0&#10;            #agent.accel = 20.0 if agent.adversary else 25.0&#10;            agent.max_speed = 1.0 if agent.adversary else 1.3&#10;        # add landmarks&#10;        world.landmarks = [Landmark() for i in range(num_landmarks)]&#10;        for i, landmark in enumerate(world.landmarks):&#10;            landmark.name = 'landmark %d' % i&#10;            landmark.collide = True&#10;            landmark.movable = False&#10;            landmark.size = 0.2&#10;            landmark.boundary = False&#10;        world.food = [Landmark() for i in range(num_food)]&#10;        for i, landmark in enumerate(world.food):&#10;            landmark.name = 'food %d' % i&#10;            landmark.collide = False&#10;            landmark.movable = False&#10;            landmark.size = 0.03&#10;            landmark.boundary = False&#10;        world.forests = [Landmark() for i in range(num_forests)]&#10;        for i, landmark in enumerate(world.forests):&#10;            landmark.name = 'forest %d' % i&#10;            landmark.collide = False&#10;            landmark.movable = False&#10;            landmark.size = 0.3&#10;            landmark.boundary = False&#10;        world.landmarks += world.food&#10;        world.landmarks += world.forests&#10;        #world.landmarks += self.set_boundaries(world)  # world boundaries now penalized with negative reward&#10;        # make initial conditions&#10;        self.reset_world(world)&#10;        return world&#10;&#10;    def set_boundaries(self, world):&#10;        boundary_list = []&#10;        landmark_size = 1&#10;        edge = 1 + landmark_size&#10;        num_landmarks = int(edge * 2 / landmark_size)&#10;        for x_pos in [-edge, edge]:&#10;            for i in range(num_landmarks):&#10;                l = Landmark()&#10;                l.state.p_pos = np.array([x_pos, -1 + i * landmark_size])&#10;                boundary_list.append(l)&#10;&#10;        for y_pos in [-edge, edge]:&#10;            for i in range(num_landmarks):&#10;                l = Landmark()&#10;                l.state.p_pos = np.array([-1 + i * landmark_size, y_pos])&#10;                boundary_list.append(l)&#10;&#10;        for i, l in enumerate(boundary_list):&#10;            l.name = 'boundary %d' % i&#10;            l.collide == True&#10;            l.movable = False&#10;            l.boundary = True&#10;            l.color = np.array([0.75, 0.75, 0.75])&#10;            l.size = landmark_size&#10;            l.state.p_vel = np.zeros(world.dim_p)&#10;&#10;        return boundary_list&#10;&#10;&#10;    def reset_world(self, world):&#10;        # random properties for agents&#10;        for i, agent in enumerate(world.agents):&#10;            agent.color = np.array([0.45, 0.95, 0.45]) if not agent.adversary else np.array([0.95, 0.45, 0.45])&#10;            # (no leader color tweak — all adversaries look the same)&#10;        for i, landmark in enumerate(world.landmarks):&#10;            landmark.color = np.array([0.25, 0.25, 0.25])&#10;        for i, landmark in enumerate(world.food):&#10;            landmark.color = np.array([0.15, 0.15, 0.65])&#10;        for i, landmark in enumerate(world.forests):&#10;            landmark.color = np.array([0.6, 0.9, 0.6])&#10;        # set random initial states&#10;        for agent in world.agents:&#10;            agent.state.p_pos = np.random.uniform(-1, +1, world.dim_p)&#10;            agent.state.p_vel = np.zeros(world.dim_p)&#10;            agent.state.c = np.zeros(world.dim_c)&#10;        for i, landmark in enumerate(world.landmarks):&#10;            landmark.state.p_pos = 0.8 * np.random.uniform(-1, +1, world.dim_p)&#10;            landmark.state.p_vel = np.zeros(world.dim_p)&#10;        for i, landmark in enumerate(world.food):&#10;            landmark.state.p_pos = 0.8 * np.random.uniform(-1, +1, world.dim_p)&#10;            landmark.state.p_vel = np.zeros(world.dim_p)&#10;        for i, landmark in enumerate(world.forests):&#10;            landmark.state.p_pos = 0.8 * np.random.uniform(-1, +1, world.dim_p)&#10;            landmark.state.p_vel = np.zeros(world.dim_p)&#10;&#10;    def benchmark_data(self, agent, world):&#10;        if agent.adversary:&#10;            collisions = 0&#10;            for a in self.good_agents(world):&#10;                if self.is_collision(a, agent):&#10;                    collisions += 1&#10;            return collisions&#10;        else:&#10;            return 0&#10;&#10;&#10;    def is_collision(self, agent1, agent2):&#10;        delta_pos = agent1.state.p_pos - agent2.state.p_pos&#10;        dist = np.sqrt(np.sum(np.square(delta_pos)))&#10;        dist_min = agent1.size + agent2.size&#10;        return True if dist &lt; dist_min else False&#10;&#10;&#10;    # return all agents that are not adversaries&#10;    def good_agents(self, world):&#10;        return [agent for agent in world.agents if not agent.adversary]&#10;&#10;    # return all adversarial agents&#10;    def adversaries(self, world):&#10;        return [agent for agent in world.agents if agent.adversary]&#10;&#10;&#10;    def reward(self, agent, world):&#10;        # Agents are rewarded based on minimum agent distance to each landmark&#10;        #boundary_reward = -10 if self.outside_boundary(agent) else 0&#10;        main_reward = self.adversary_reward(agent, world) if agent.adversary else self.agent_reward(agent, world)&#10;        return main_reward&#10;&#10;    def outside_boundary(self, agent):&#10;        if agent.state.p_pos[0] &gt; 1 or agent.state.p_pos[0] &lt; -1 or agent.state.p_pos[1] &gt; 1 or agent.state.p_pos[1] &lt; -1:&#10;            return True&#10;        else:&#10;            return False&#10;&#10;&#10;    def agent_reward(self, agent, world):&#10;        # Agents are rewarded based on minimum agent distance to each landmark&#10;        rew = 0&#10;        shape = False&#10;        adversaries = self.adversaries(world)&#10;        if shape:&#10;            for adv in adversaries:&#10;                rew += 0.1 * np.sqrt(np.sum(np.square(agent.state.p_pos - adv.state.p_pos)))&#10;        if agent.collide:&#10;            for a in adversaries:&#10;                if self.is_collision(a, agent):&#10;                    rew -= 5&#10;        def bound(x):&#10;            if x &lt; 0.9:&#10;                return 0&#10;            if x &lt; 1.0:&#10;                return (x - 0.9) * 10&#10;            return min(np.exp(2 * x - 2), 10)  # 1 + (x - 1) * (x - 1)&#10;&#10;        for p in range(world.dim_p):&#10;            x = abs(agent.state.p_pos[p])&#10;            rew -= 2 * bound(x)&#10;&#10;        for food in world.food:&#10;            if self.is_collision(agent, food):&#10;                rew += 2&#10;        rew -= 0.05 * min([np.sqrt(np.sum(np.square(food.state.p_pos - agent.state.p_pos))) for food in world.food])&#10;&#10;        return rew&#10;&#10;    def adversary_reward(self, agent, world):&#10;        # Adversaries are rewarded for collisions with agents&#10;        rew = 0&#10;        shape = True&#10;        agents = self.good_agents(world)&#10;        adversaries = self.adversaries(world)&#10;        if shape:&#10;            rew -= 0.1 * min([np.sqrt(np.sum(np.square(a.state.p_pos - agent.state.p_pos))) for a in agents])&#10;        if agent.collide:&#10;            for ag in agents:&#10;                for adv in adversaries:&#10;                    if self.is_collision(ag, adv):&#10;                        rew += 5&#10;        return rew&#10;&#10;&#10;    def observation2(self, agent, world):&#10;        # get positions of all entities in this agent's reference frame&#10;        entity_pos = []&#10;        for entity in world.landmarks:  # world.entities:&#10;            if not entity.boundary:&#10;                entity_pos.append(entity.state.p_pos - agent.state.p_pos)&#10;&#10;        food_pos = []&#10;        for entity in world.food:  # world.entities:&#10;            if not entity.boundary:&#10;                food_pos.append(entity.state.p_pos - agent.state.p_pos)&#10;        # communication of all other agents&#10;        comm = []&#10;        other_pos = []&#10;        other_vel = []&#10;        for other in world.agents:&#10;            if other is agent: continue&#10;            comm.append(other.state.c)&#10;            other_pos.append(other.state.p_pos - agent.state.p_pos)&#10;            if not other.adversary:&#10;                other_vel.append(other.state.p_vel)&#10;        return np.concatenate([agent.state.p_vel] + [agent.state.p_pos] + entity_pos + other_pos + other_vel)&#10;&#10;    def observation(self, agent, world):&#10;        # get positions of all entities in this agent's reference frame&#10;        entity_pos = []&#10;        for entity in world.landmarks:&#10;            if not entity.boundary:&#10;                entity_pos.append(entity.state.p_pos - agent.state.p_pos)&#10;&#10;        in_forest = [np.array([-1]), np.array([-1])]&#10;        inf1 = False&#10;        inf2 = False&#10;        if self.is_collision(agent, world.forests[0]):&#10;            in_forest[0] = np.array([1])&#10;            inf1= True&#10;        if self.is_collision(agent, world.forests[1]):&#10;            in_forest[1] = np.array([1])&#10;            inf2 = True&#10;&#10;        food_pos = []&#10;        for entity in world.food:&#10;            if not entity.boundary:&#10;                food_pos.append(entity.state.p_pos - agent.state.p_pos)&#10;&#10;        # other agents — forest-based visibility (no leader bypass)&#10;        other_pos = []&#10;        other_vel = []&#10;        for other in world.agents:&#10;            if other is agent: continue&#10;            oth_f1 = self.is_collision(other, world.forests[0])&#10;            oth_f2 = self.is_collision(other, world.forests[1])&#10;            if (inf1 and oth_f1) or (inf2 and oth_f2) or (not inf1 and not oth_f1 and not inf2 and not oth_f2):&#10;                other_pos.append(other.state.p_pos - agent.state.p_pos)&#10;                if not other.adversary:&#10;                    other_vel.append(other.state.p_vel)&#10;            else:&#10;                other_pos.append([0, 0])&#10;                if not other.adversary:&#10;                    other_vel.append([0, 0])&#10;&#10;        # prey_forest: tell each adversary whether each prey is hiding&#10;        prey_forest = []&#10;        ga = self.good_agents(world)&#10;        for a in ga:&#10;            if any([self.is_collision(a, f) for f in world.forests]):&#10;                prey_forest.append(np.array([1]))&#10;            else:&#10;                prey_forest.append(np.array([-1]))&#10;&#10;        # ---- return observations ----&#10;        if agent.adversary:&#10;            # Adversary obs (32-d):&#10;            #   [p_vel(2)|p_pos(2)|entity_pos(10)|other_pos(10)|good_vel(4)|in_forest(2)|prey_forest(2)]&#10;            return np.concatenate(&#10;                [agent.state.p_vel] + [agent.state.p_pos] + entity_pos&#10;                + other_pos + other_vel + in_forest + prey_forest)&#10;        else:&#10;            # Good-agent obs (28-d):&#10;            #   [p_vel(2)|p_pos(2)|entity_pos(10)|other_pos(10)|in_forest(2)|other_vel(2)]&#10;            return np.concatenate(&#10;                [agent.state.p_vel] + [agent.state.p_pos] + entity_pos&#10;                + other_pos + in_forest + other_vel)&#10;" />
              <option name="updatedContent" value="import numpy as np&#10;from onpolicy.envs.mpe.core import World, Agent, Landmark&#10;from onpolicy.envs.mpe.scenario import BaseScenario&#10;&#10;class Scenario(BaseScenario):&#10;    def make_world(self,args):&#10;        world = World()&#10;        # set any world properties first&#10;        world.dim_c = 4&#10;        #world.damping = 1&#10;        num_good_agents = args.num_good_agents#2&#10;        num_adversaries = args.num_adversaries#4&#10;        num_agents = num_adversaries + num_good_agents&#10;        num_landmarks = args.num_landmarks#1&#10;        num_food = 2&#10;        num_forests = 2&#10;        # add agents&#10;        world.agents = [Agent() for i in range(num_agents)]&#10;        for i, agent in enumerate(world.agents):&#10;            agent.name = 'agent %d' % i&#10;            agent.collide = True&#10;            agent.leader = False          # no leader — all adversaries are equal&#10;            agent.silent = True           # no comm action — all Discrete(5)&#10;            agent.adversary = True if i &lt; num_adversaries else False&#10;            agent.size = 0.075 if agent.adversary else 0.045&#10;            agent.accel = 3.0 if agent.adversary else 4.0&#10;            #agent.accel = 20.0 if agent.adversary else 25.0&#10;            agent.max_speed = 1.0 if agent.adversary else 1.3&#10;        # add landmarks&#10;        world.landmarks = [Landmark() for i in range(num_landmarks)]&#10;        for i, landmark in enumerate(world.landmarks):&#10;            landmark.name = 'landmark %d' % i&#10;            landmark.collide = True&#10;            landmark.movable = False&#10;            landmark.size = 0.2&#10;            landmark.boundary = False&#10;        world.food = [Landmark() for i in range(num_food)]&#10;        for i, landmark in enumerate(world.food):&#10;            landmark.name = 'food %d' % i&#10;            landmark.collide = False&#10;            landmark.movable = False&#10;            landmark.size = 0.03&#10;            landmark.boundary = False&#10;        world.forests = [Landmark() for i in range(num_forests)]&#10;        for i, landmark in enumerate(world.forests):&#10;            landmark.name = 'forest %d' % i&#10;            landmark.collide = False&#10;            landmark.movable = False&#10;            landmark.size = 0.3&#10;            landmark.boundary = False&#10;        world.landmarks += world.food&#10;        world.landmarks += world.forests&#10;        #world.landmarks += self.set_boundaries(world)  # world boundaries now penalized with negative reward&#10;        # make initial conditions&#10;        self.reset_world(world)&#10;        return world&#10;&#10;    def set_boundaries(self, world):&#10;        boundary_list = []&#10;        landmark_size = 1&#10;        edge = 1 + landmark_size&#10;        num_landmarks = int(edge * 2 / landmark_size)&#10;        for x_pos in [-edge, edge]:&#10;            for i in range(num_landmarks):&#10;                l = Landmark()&#10;                l.state.p_pos = np.array([x_pos, -1 + i * landmark_size])&#10;                boundary_list.append(l)&#10;&#10;        for y_pos in [-edge, edge]:&#10;            for i in range(num_landmarks):&#10;                l = Landmark()&#10;                l.state.p_pos = np.array([-1 + i * landmark_size, y_pos])&#10;                boundary_list.append(l)&#10;&#10;        for i, l in enumerate(boundary_list):&#10;            l.name = 'boundary %d' % i&#10;            l.collide == True&#10;            l.movable = False&#10;            l.boundary = True&#10;            l.color = np.array([0.75, 0.75, 0.75])&#10;            l.size = landmark_size&#10;            l.state.p_vel = np.zeros(world.dim_p)&#10;&#10;        return boundary_list&#10;&#10;&#10;    def reset_world(self, world):&#10;        # random properties for agents&#10;        for i, agent in enumerate(world.agents):&#10;            agent.color = np.array([0.45, 0.95, 0.45]) if not agent.adversary else np.array([0.95, 0.45, 0.45])&#10;            # (no leader color tweak — all adversaries look the same)&#10;        for i, landmark in enumerate(world.landmarks):&#10;            landmark.color = np.array([0.25, 0.25, 0.25])&#10;        for i, landmark in enumerate(world.food):&#10;            landmark.color = np.array([0.15, 0.15, 0.65])&#10;        for i, landmark in enumerate(world.forests):&#10;            landmark.color = np.array([0.6, 0.9, 0.6])&#10;        # set random initial states&#10;        for agent in world.agents:&#10;            agent.state.p_pos = np.random.uniform(-1, +1, world.dim_p)&#10;            agent.state.p_vel = np.zeros(world.dim_p)&#10;            agent.state.c = np.zeros(world.dim_c)&#10;        for i, landmark in enumerate(world.landmarks):&#10;            landmark.state.p_pos = 0.8 * np.random.uniform(-1, +1, world.dim_p)&#10;            landmark.state.p_vel = np.zeros(world.dim_p)&#10;        for i, landmark in enumerate(world.food):&#10;            landmark.state.p_pos = 0.8 * np.random.uniform(-1, +1, world.dim_p)&#10;            landmark.state.p_vel = np.zeros(world.dim_p)&#10;        for i, landmark in enumerate(world.forests):&#10;            landmark.state.p_pos = 0.8 * np.random.uniform(-1, +1, world.dim_p)&#10;            landmark.state.p_vel = np.zeros(world.dim_p)&#10;&#10;    def benchmark_data(self, agent, world):&#10;        if agent.adversary:&#10;            collisions = 0&#10;            for a in self.good_agents(world):&#10;                if self.is_collision(a, agent):&#10;                    collisions += 1&#10;            return collisions&#10;        else:&#10;            return 0&#10;&#10;&#10;    def is_collision(self, agent1, agent2):&#10;        delta_pos = agent1.state.p_pos - agent2.state.p_pos&#10;        dist = np.sqrt(np.sum(np.square(delta_pos)))&#10;        dist_min = agent1.size + agent2.size&#10;        return True if dist &lt; dist_min else False&#10;&#10;&#10;    # return all agents that are not adversaries&#10;    def good_agents(self, world):&#10;        return [agent for agent in world.agents if not agent.adversary]&#10;&#10;    # return all adversarial agents&#10;    def adversaries(self, world):&#10;        return [agent for agent in world.agents if agent.adversary]&#10;&#10;&#10;    def reward(self, agent, world):&#10;        # Agents are rewarded based on minimum agent distance to each landmark&#10;        #boundary_reward = -10 if self.outside_boundary(agent) else 0&#10;        main_reward = self.adversary_reward(agent, world) if agent.adversary else self.agent_reward(agent, world)&#10;        return main_reward&#10;&#10;    def outside_boundary(self, agent):&#10;        if agent.state.p_pos[0] &gt; 1 or agent.state.p_pos[0] &lt; -1 or agent.state.p_pos[1] &gt; 1 or agent.state.p_pos[1] &lt; -1:&#10;            return True&#10;        else:&#10;            return False&#10;&#10;&#10;    def agent_reward(self, agent, world):&#10;        # Agents are rewarded based on minimum agent distance to each landmark&#10;        rew = 0&#10;        shape = True&#10;        adversaries = self.adversaries(world)&#10;        if shape:&#10;            for adv in adversaries:&#10;                rew += 0.1 * np.sqrt(np.sum(np.square(agent.state.p_pos - adv.state.p_pos)))&#10;        if agent.collide:&#10;            for a in adversaries:&#10;                if self.is_collision(a, agent):&#10;                    rew -= 5&#10;        def bound(x):&#10;            if x &lt; 0.9:&#10;                return 0&#10;            if x &lt; 1.0:&#10;                return (x - 0.9) * 10&#10;            return min(np.exp(2 * x - 2), 10)  # 1 + (x - 1) * (x - 1)&#10;&#10;        for p in range(world.dim_p):&#10;            x = abs(agent.state.p_pos[p])&#10;            rew -= 2 * bound(x)&#10;&#10;        for food in world.food:&#10;            if self.is_collision(agent, food):&#10;                rew += 2&#10;        rew -= 0.05 * min([np.sqrt(np.sum(np.square(food.state.p_pos - agent.state.p_pos))) for food in world.food])&#10;&#10;        return rew&#10;&#10;    def adversary_reward(self, agent, world):&#10;        # Adversaries are rewarded for collisions with agents&#10;        rew = 0&#10;        shape = True&#10;        agents = self.good_agents(world)&#10;        adversaries = self.adversaries(world)&#10;        if shape:&#10;            rew -= 0.1 * min([np.sqrt(np.sum(np.square(a.state.p_pos - agent.state.p_pos))) for a in agents])&#10;        if agent.collide:&#10;            for ag in agents:&#10;                for adv in adversaries:&#10;                    if self.is_collision(ag, adv):&#10;                        rew += 5&#10;        return rew&#10;&#10;&#10;    def observation2(self, agent, world):&#10;        # get positions of all entities in this agent's reference frame&#10;        entity_pos = []&#10;        for entity in world.landmarks:  # world.entities:&#10;            if not entity.boundary:&#10;                entity_pos.append(entity.state.p_pos - agent.state.p_pos)&#10;&#10;        food_pos = []&#10;        for entity in world.food:  # world.entities:&#10;            if not entity.boundary:&#10;                food_pos.append(entity.state.p_pos - agent.state.p_pos)&#10;        # communication of all other agents&#10;        comm = []&#10;        other_pos = []&#10;        other_vel = []&#10;        for other in world.agents:&#10;            if other is agent: continue&#10;            comm.append(other.state.c)&#10;            other_pos.append(other.state.p_pos - agent.state.p_pos)&#10;            if not other.adversary:&#10;                other_vel.append(other.state.p_vel)&#10;        return np.concatenate([agent.state.p_vel] + [agent.state.p_pos] + entity_pos + other_pos + other_vel)&#10;&#10;    def observation(self, agent, world):&#10;        # get positions of all entities in this agent's reference frame&#10;        entity_pos = []&#10;        for entity in world.landmarks:&#10;            if not entity.boundary:&#10;                entity_pos.append(entity.state.p_pos - agent.state.p_pos)&#10;&#10;        in_forest = [np.array([-1]), np.array([-1])]&#10;        inf1 = False&#10;        inf2 = False&#10;        if self.is_collision(agent, world.forests[0]):&#10;            in_forest[0] = np.array([1])&#10;            inf1= True&#10;        if self.is_collision(agent, world.forests[1]):&#10;            in_forest[1] = np.array([1])&#10;            inf2 = True&#10;&#10;        food_pos = []&#10;        for entity in world.food:&#10;            if not entity.boundary:&#10;                food_pos.append(entity.state.p_pos - agent.state.p_pos)&#10;&#10;        # other agents — forest-based visibility (no leader bypass)&#10;        other_pos = []&#10;        other_vel = []&#10;        for other in world.agents:&#10;            if other is agent: continue&#10;            oth_f1 = self.is_collision(other, world.forests[0])&#10;            oth_f2 = self.is_collision(other, world.forests[1])&#10;            if (inf1 and oth_f1) or (inf2 and oth_f2) or (not inf1 and not oth_f1 and not inf2 and not oth_f2):&#10;                other_pos.append(other.state.p_pos - agent.state.p_pos)&#10;                if not other.adversary:&#10;                    other_vel.append(other.state.p_vel)&#10;            else:&#10;                other_pos.append([0, 0])&#10;                if not other.adversary:&#10;                    other_vel.append([0, 0])&#10;&#10;        # prey_forest: tell each adversary whether each prey is hiding&#10;        prey_forest = []&#10;        ga = self.good_agents(world)&#10;        for a in ga:&#10;            if any([self.is_collision(a, f) for f in world.forests]):&#10;                prey_forest.append(np.array([1]))&#10;            else:&#10;                prey_forest.append(np.array([-1]))&#10;&#10;        # ---- return observations ----&#10;        if agent.adversary:&#10;            # Adversary obs (32-d):&#10;            #   [p_vel(2)|p_pos(2)|entity_pos(10)|other_pos(10)|good_vel(4)|in_forest(2)|prey_forest(2)]&#10;            return np.concatenate(&#10;                [agent.state.p_vel] + [agent.state.p_pos] + entity_pos&#10;                + other_pos + other_vel + in_forest + prey_forest)&#10;        else:&#10;            # Good-agent obs (28-d):&#10;            #   [p_vel(2)|p_pos(2)|entity_pos(10)|other_pos(10)|in_forest(2)|other_vel(2)]&#10;            return np.concatenate(&#10;                [agent.state.p_vel] + [agent.state.p_pos] + entity_pos&#10;                + other_pos + in_forest + other_vel)&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="/vaggp_trainer (snapshot).py">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/vaggp_trainer (snapshot).py" />
              <option name="originalContent" value="&quot;&quot;&quot;&#10;VAGGP_Trainer  -  PPO trainer with 4-term composite loss for VA-GGP&#10;====================================================================&#10;Drop-in replacement for R_MAPPO when training adversary agents with&#10;the Variational Autoregressive Guided Graph Policy architecture.&#10;&#10;Composite loss (per mini-batch):&#10;  L_total = L_distill  +  lambda_rl * L_rl  +  beta_recon * L_recon&#10;            + beta_kl * L_kl_bottleneck  -  entropy_coef * H(pi)&#10;&#10;where:&#10;  L_rl       = standard PPO-Clip surrogate&#10;  L_distill  = MAGPO double-clipped KL  (learner -&gt; guider)&#10;  L_recon    = MSE  decoder(z) vs. ground-truth prey relative positions&#10;  L_kl       = KL( q(z|obs) || N(0,I) )&#10;&quot;&quot;&quot;&#10;import numpy as np&#10;import torch&#10;import torch.nn as nn&#10;import torch.nn.functional as F&#10;&#10;from onpolicy.utils.util import get_gard_norm, huber_loss, mse_loss&#10;from onpolicy.utils.valuenorm import ValueNorm&#10;from onpolicy.algorithms.utils.util import check&#10;from onpolicy.algorithms.r_mappo.algorithm.adversary_vaggp_actor import (&#10;    VariationalBottleneck,&#10;    double_clip_distillation_loss,&#10;)&#10;&#10;&#10;class VAGGP_Trainer:&#10;    &quot;&quot;&quot;&#10;    PPO trainer extended with VA-GGP composite loss for adversary agents.&#10;&#10;    Identical to R_MAPPO except ppo_update() computes 4 loss terms&#10;    and jointly optimises learner + guider + critic.&#10;    &quot;&quot;&quot;&#10;&#10;    def __init__(self, args, policy, device=torch.device(&quot;cpu&quot;)):&#10;        self.device = device&#10;        self.tpdv = dict(dtype=torch.float32, device=device)&#10;        self.policy = policy&#10;&#10;        # ---- standard PPO hyper-parameters ----&#10;        self.clip_param = args.clip_param&#10;        self.ppo_epoch = args.ppo_epoch&#10;        self.num_mini_batch = args.num_mini_batch&#10;        self.data_chunk_length = args.data_chunk_length&#10;        self.value_loss_coef = args.value_loss_coef&#10;        self.entropy_coef = args.entropy_coef&#10;        self.max_grad_norm = args.max_grad_norm&#10;        self.huber_delta = args.huber_delta&#10;&#10;        self._use_recurrent_policy = args.use_recurrent_policy&#10;        self._use_naive_recurrent = args.use_naive_recurrent_policy&#10;        self._use_max_grad_norm = args.use_max_grad_norm&#10;        self._use_clipped_value_loss = args.use_clipped_value_loss&#10;        self._use_huber_loss = args.use_huber_loss&#10;        self._use_popart = args.use_popart&#10;        self._use_valuenorm = args.use_valuenorm&#10;        self._use_value_active_masks = args.use_value_active_masks&#10;        self._use_policy_active_masks = args.use_policy_active_masks&#10;&#10;        # ---- VA-GGP specific hyper-parameters ----&#10;        self.lambda_rl = getattr(args, 'vaggp_lambda_rl', 1.0)&#10;        self.beta_recon = getattr(args, 'vaggp_beta_recon', 0.1)&#10;        self.beta_kl = getattr(args, 'vaggp_beta_kl', 0.01)&#10;        self.delta_clip = getattr(args, 'vaggp_delta_clip', 1.5)&#10;&#10;        # ---- value normaliser ----&#10;        assert not (self._use_popart and self._use_valuenorm)&#10;        if self._use_popart:&#10;            self.value_normalizer = self.policy.critic.v_out&#10;        elif self._use_valuenorm:&#10;            self.value_normalizer = ValueNorm(1, device=self.device)&#10;        else:&#10;            self.value_normalizer = None&#10;&#10;    # ==================================================================&#10;    #  Value loss  (identical to R_MAPPO)&#10;    # ==================================================================&#10;    def cal_value_loss(self, values, value_preds_batch,&#10;                       return_batch, active_masks_batch):&#10;        value_pred_clipped = value_preds_batch + (&#10;            values - value_preds_batch&#10;        ).clamp(-self.clip_param, self.clip_param)&#10;&#10;        if self._use_popart or self._use_valuenorm:&#10;            self.value_normalizer.update(return_batch)&#10;            error_clipped = (&#10;                self.value_normalizer.normalize(return_batch) - value_pred_clipped)&#10;            error_original = (&#10;                self.value_normalizer.normalize(return_batch) - values)&#10;        else:&#10;            error_clipped = return_batch - value_pred_clipped&#10;            error_original = return_batch - values&#10;&#10;        if self._use_huber_loss:&#10;            vloss_c = huber_loss(error_clipped, self.huber_delta)&#10;            vloss_o = huber_loss(error_original, self.huber_delta)&#10;        else:&#10;            vloss_c = mse_loss(error_clipped)&#10;            vloss_o = mse_loss(error_original)&#10;&#10;        value_loss = (torch.max(vloss_o, vloss_c)&#10;                      if self._use_clipped_value_loss else vloss_o)&#10;&#10;        if self._use_value_active_masks:&#10;            value_loss = ((value_loss * active_masks_batch).sum()&#10;                          / active_masks_batch.sum())&#10;        else:&#10;            value_loss = value_loss.mean()&#10;        return value_loss&#10;&#10;    # ==================================================================&#10;    #  Composite PPO update  (the core of VA-GGP)&#10;    # ==================================================================&#10;    def ppo_update(self, sample, update_actor=True,&#10;                   prev_adv_actions_onehot=None):&#10;        &quot;&quot;&quot;&#10;        One gradient step on the 4-term composite loss.&#10;&#10;        Args:&#10;            sample:  standard PPO batch tuple from the buffer generator&#10;            update_actor:  whether to update the learner actor&#10;            prev_adv_actions_onehot:  list of [B, MAX_OH] tensors for the&#10;                autoregressive guider context, or None (first adversary&#10;                in the chain / guider disabled)&#10;&#10;        Returns:&#10;            (value_loss, critic_grad_norm, policy_loss, dist_entropy,&#10;             actor_grad_norm, imp_weights, diag_dict)&#10;        &quot;&quot;&quot;&#10;        # ---- unpack sample ----&#10;        if len(sample) == 12:&#10;            (share_obs_batch, obs_batch, rnn_states_batch,&#10;             rnn_states_critic_batch, actions_batch, value_preds_batch,&#10;             return_batch, masks_batch, active_masks_batch,&#10;             old_action_log_probs_batch, adv_targ,&#10;             available_actions_batch) = sample&#10;        else:&#10;            (share_obs_batch, obs_batch, rnn_states_batch,&#10;             rnn_states_critic_batch, actions_batch, value_preds_batch,&#10;             return_batch, masks_batch, active_masks_batch,&#10;             old_action_log_probs_batch, adv_targ,&#10;             available_actions_batch, _) = sample&#10;&#10;        old_action_log_probs_batch = check(old_action_log_probs_batch).to(**self.tpdv)&#10;        adv_targ = check(adv_targ).to(**self.tpdv)&#10;        value_preds_batch = check(value_preds_batch).to(**self.tpdv)&#10;        return_batch = check(return_batch).to(**self.tpdv)&#10;        active_masks_batch = check(active_masks_batch).to(**self.tpdv)&#10;&#10;        # ==============================================================&#10;        #  Forward pass:  learner evaluate_actions  (returns vaggp_info)&#10;        # ==============================================================&#10;        action_log_probs, dist_entropy, vaggp_info = \&#10;            self.policy.actor.evaluate_actions(&#10;                obs_batch, rnn_states_batch, actions_batch,&#10;                masks_batch, available_actions_batch, active_masks_batch)&#10;&#10;        values, _ = self.policy.critic(&#10;            check(share_obs_batch).to(**self.tpdv),&#10;            check(rnn_states_critic_batch).to(**self.tpdv),&#10;            check(masks_batch).to(**self.tpdv))&#10;&#10;        # ==============================================================&#10;        #  Loss 1:  Auxiliary RL  (standard PPO-Clip)&#10;        # ==============================================================&#10;        imp_weights = torch.exp(&#10;            action_log_probs - old_action_log_probs_batch)       # [B, 1]&#10;&#10;        surr1 = imp_weights * adv_targ&#10;        surr2 = torch.clamp(&#10;            imp_weights,&#10;            1.0 - self.clip_param,&#10;            1.0 + self.clip_param) * adv_targ&#10;&#10;        if self._use_policy_active_masks:&#10;            rl_loss = (&#10;                (-torch.sum(torch.min(surr1, surr2), dim=-1, keepdim=True)&#10;                 * active_masks_batch).sum() / active_masks_batch.sum())&#10;        else:&#10;            rl_loss = -torch.sum(&#10;                torch.min(surr1, surr2), dim=-1, keepdim=True).mean()&#10;&#10;        # ==============================================================&#10;        #  Loss 2:  MAGPO distillation  (double-clipped KL)&#10;        # ==============================================================&#10;        if (self.policy.guider is not None&#10;                and prev_adv_actions_onehot is not None):&#10;            # Guider produces reference probs (no gradient through guider)&#10;            with torch.no_grad():&#10;                guider_probs = self.policy.guider(&#10;                    share_obs_batch, prev_adv_actions_onehot,&#10;                    available_actions_batch)                       # [B, A]&#10;&#10;            # Learner full log-probs (need grad for distillation)&#10;            learner_logprobs = self.policy.actor.get_probs(&#10;                obs_batch, rnn_states_batch, masks_batch,&#10;                available_actions_batch).log()                    # [B, A]&#10;&#10;            distill_loss = double_clip_distillation_loss(&#10;                learner_logprobs, guider_probs, delta=self.delta_clip)&#10;        else:&#10;            distill_loss = torch.tensor(0.0, device=self.device)&#10;&#10;        # ==============================================================&#10;        #  Loss 3:  VGBSM reconstruction&#10;        #    decoder(z) should predict prey relative positions&#10;        # ==============================================================&#10;        decoded = vaggp_info['decoded']        # [B, num_good*2]&#10;        target = vaggp_info['target']          # [B, num_good*2]&#10;        recon_loss = F.mse_loss(decoded, target.detach())&#10;&#10;        # ==============================================================&#10;        #  Loss 4:  VGBSM KL bottleneck&#10;        #    KL( q(z|obs) || N(0,I) )&#10;        # ==============================================================&#10;        kl_bottleneck = VariationalBottleneck.kl_divergence(&#10;            vaggp_info['mu'], vaggp_info['logvar'])&#10;&#10;        # ==============================================================&#10;        #  Total actor loss  (4 terms + entropy bonus)&#10;        # ==============================================================&#10;        total_actor_loss = (&#10;            distill_loss&#10;            + self.lambda_rl * rl_loss&#10;            + self.beta_recon * recon_loss&#10;            + self.beta_kl * kl_bottleneck&#10;            - self.entropy_coef * dist_entropy&#10;        )&#10;&#10;        # ---- Backward + step  (learner) ----&#10;        self.policy.actor_optimizer.zero_grad()&#10;        if update_actor:&#10;            total_actor_loss.backward()&#10;        if self._use_max_grad_norm:&#10;            actor_grad_norm = nn.utils.clip_grad_norm_(&#10;                self.policy.actor.parameters(), self.max_grad_norm)&#10;        else:&#10;            actor_grad_norm = get_gard_norm(self.policy.actor.parameters())&#10;        self.policy.actor_optimizer.step()&#10;&#10;        # ---- Guider supervised update  (maximise log-prob of buffered actions) ----&#10;        if (self.policy.guider is not None&#10;                and prev_adv_actions_onehot is not None):&#10;            guider_lp, guider_ent = self.policy.guider.get_log_probs_and_entropy(&#10;                share_obs_batch, prev_adv_actions_onehot,&#10;                actions_batch, available_actions_batch, active_masks_batch)&#10;            guider_loss = -guider_lp.mean() - 0.01 * guider_ent&#10;&#10;            self.policy.guider_optimizer.zero_grad()&#10;            guider_loss.backward()&#10;            if self._use_max_grad_norm:&#10;                nn.utils.clip_grad_norm_(&#10;                    self.policy.guider.parameters(), self.max_grad_norm)&#10;            self.policy.guider_optimizer.step()&#10;&#10;        # ---- Critic update ----&#10;        value_loss = self.cal_value_loss(&#10;            values, value_preds_batch, return_batch, active_masks_batch)&#10;&#10;        self.policy.critic_optimizer.zero_grad()&#10;        (value_loss * self.value_loss_coef).backward()&#10;        if self._use_max_grad_norm:&#10;            critic_grad_norm = nn.utils.clip_grad_norm_(&#10;                self.policy.critic.parameters(), self.max_grad_norm)&#10;        else:&#10;            critic_grad_norm = get_gard_norm(self.policy.critic.parameters())&#10;        self.policy.critic_optimizer.step()&#10;&#10;        # ---- diagnostics dict ----&#10;        diag = dict(&#10;            recon_loss=recon_loss.item(),&#10;            kl_bottleneck=kl_bottleneck.item(),&#10;            distill_loss=(distill_loss.item()&#10;                          if torch.is_tensor(distill_loss)&#10;                          else distill_loss),&#10;        )&#10;        return (value_loss, critic_grad_norm, rl_loss, dist_entropy,&#10;                actor_grad_norm, imp_weights, diag)&#10;&#10;    # ==================================================================&#10;    #  Step 4: Policy back-tracking (representation sync)&#10;    # ==================================================================&#10;    def backtrack_sync(self):&#10;        &quot;&quot;&quot;&#10;        Copy compatible weights from the learner's graph-frontend&#10;        fusion layers into the guider's global encoder, keeping the&#10;        two representation spaces roughly aligned.&#10;        &quot;&quot;&quot;&#10;        if self.policy.guider is None:&#10;            return&#10;        src = self.policy.actor.graph.fusion.state_dict()&#10;        tgt = self.policy.guider.global_enc.state_dict()&#10;        for key in tgt:&#10;            if key in src and src[key].shape == tgt[key].shape:&#10;                tgt[key].copy_(src[key])&#10;        self.policy.guider.global_enc.load_state_dict(tgt)&#10;&#10;    # ==================================================================&#10;    #  Full training loop  (ppo_epoch x mini_batch)&#10;    # ==================================================================&#10;    def train(self, buffer, update_actor=True,&#10;              all_adv_buffers=None, adv_order_idx=0,&#10;              ar_prev_agents=None):&#10;        &quot;&quot;&quot;&#10;        Full training cycle.&#10;&#10;        Args:&#10;            buffer:           this agent's SeparatedReplayBuffer&#10;            update_actor:     whether to update the learner actor&#10;            all_adv_buffers:  list of all adversary SeparatedReplayBuffers&#10;                              (needed to build autoregressive context)&#10;            adv_order_idx:    this agent's position in the adversary ordering&#10;            ar_prev_agents:   (list[int] | None) shuffled list of adversary&#10;                              agent-IDs that precede this agent in the&#10;                              current AR ordering.  When provided, overrides&#10;                              the fixed 0..adv_order_idx-1 ordering.&#10;        &quot;&quot;&quot;&#10;        # ---- compute advantages ----&#10;        if self._use_popart or self._use_valuenorm:&#10;            advantages = (buffer.returns[:-1]&#10;                          - self.value_normalizer.denormalize(&#10;                              buffer.value_preds[:-1]))&#10;        else:&#10;            advantages = buffer.returns[:-1] - buffer.value_preds[:-1]&#10;        adv_copy = advantages.copy()&#10;        adv_copy[buffer.active_masks[:-1] == 0.0] = np.nan&#10;        mean_adv = np.nanmean(adv_copy)&#10;        std_adv = np.nanstd(adv_copy)&#10;        advantages = (advantages - mean_adv) / (std_adv + 1e-5)&#10;&#10;        # ---- pre-compute flattened prev-agent one-hot arrays ----&#10;        # Each is np.ndarray (T*N, MAX_OH) matching the buffer's flat ordering&#10;        prev_oh_flat_list = None&#10;        if all_adv_buffers is not None:&#10;            # AR Order Shuffling: use the shuffled prev-agent list when provided&#10;            if ar_prev_agents is not None and len(ar_prev_agents) &gt; 0:&#10;                prev_oh_flat_list = self._build_prev_onehot_from_list(&#10;                    all_adv_buffers, ar_prev_agents)&#10;            elif ar_prev_agents is None and adv_order_idx &gt; 0:&#10;                # Fallback to fixed order for backward compatibility&#10;                prev_oh_flat_list = self._build_prev_onehot(&#10;                    all_adv_buffers, adv_order_idx)&#10;&#10;        # ---- training loop ----&#10;        train_info = dict(&#10;            value_loss=0, policy_loss=0, dist_entropy=0,&#10;            actor_grad_norm=0, critic_grad_norm=0, ratio=0,&#10;            recon_loss=0, kl_bottleneck=0, distill_loss=0)&#10;&#10;        for _ in range(self.ppo_epoch):&#10;            # Use custom indexed generator so prev_oh aligns with sample&#10;            gen = self._indexed_ff_generator(&#10;                buffer, advantages, self.num_mini_batch,&#10;                prev_oh_flat_list)&#10;&#10;            for sample, prev_oh_batch in gen:&#10;                ctx = None&#10;                if prev_oh_batch is not None:&#10;                    ctx = [torch.from_numpy(t) for t in prev_oh_batch]&#10;&#10;                result = self.ppo_update(&#10;                    sample, update_actor,&#10;                    prev_adv_actions_onehot=ctx)&#10;&#10;                (value_loss, critic_grad_norm, policy_loss,&#10;                 dist_entropy, actor_grad_norm, imp_weights, diag) = result&#10;&#10;                train_info['value_loss'] += value_loss.item()&#10;                train_info['policy_loss'] += policy_loss.item()&#10;                train_info['dist_entropy'] += dist_entropy.item()&#10;                train_info['actor_grad_norm'] += actor_grad_norm&#10;                train_info['critic_grad_norm'] += critic_grad_norm&#10;                train_info['ratio'] += imp_weights.mean()&#10;                train_info['recon_loss'] += diag['recon_loss']&#10;                train_info['kl_bottleneck'] += diag['kl_bottleneck']&#10;                train_info['distill_loss'] += diag['distill_loss']&#10;&#10;        num_updates = self.ppo_epoch * self.num_mini_batch&#10;        for k in train_info:&#10;            train_info[k] /= num_updates&#10;&#10;        # Step 4: back-tracking sync after each training cycle&#10;        self.backtrack_sync()&#10;        return train_info&#10;&#10;    # ==================================================================&#10;    #  Helpers&#10;    # ==================================================================&#10;    @staticmethod&#10;    def _build_prev_onehot(all_adv_buffers, adv_order_idx):&#10;        &quot;&quot;&quot;&#10;        Build one-hot action arrays for adversary agents 0..adv_order_idx-1.&#10;&#10;        Returns a list of np.ndarray [T*N, MAX_OH=9] (one per prev agent),&#10;        where T=episode_length, N=n_rollout_threads.&#10;&#10;        NOTE: buf.actions has shape (episode_length, N, act_dim) — NO +1,&#10;              so we do NOT strip the last row.&#10;        &quot;&quot;&quot;&#10;        MAX_OH = 5   # all adversaries are Discrete(5), no leader comm&#10;        result = []&#10;        for prev_idx in range(adv_order_idx):&#10;            buf = all_adv_buffers[prev_idx]&#10;            # buf.actions shape: (episode_length, n_threads, act_dim)&#10;            acts = buf.actions                          # (T, N, act_dim)&#10;            flat = acts.reshape(-1, acts.shape[-1])     # (T*N, act_dim)&#10;            act_dim = flat.shape[-1]&#10;&#10;            if act_dim == 1:&#10;                # Discrete(5): single integer action -&gt; one-hot(5)&#10;                # (all adversaries are equal — no leader, all silent)&#10;                oh = np.zeros((flat.shape[0], 5), dtype=np.float32)&#10;                idx = flat[:, 0].astype(int).clip(0, 4)&#10;                oh[np.arange(len(idx)), idx] = 1.0&#10;            else:&#10;                oh = np.zeros((flat.shape[0], MAX_OH), dtype=np.float32)&#10;&#10;            # Pad to MAX_OH if needed&#10;            if oh.shape[-1] &lt; MAX_OH:&#10;                oh = np.pad(oh, ((0, 0), (0, MAX_OH - oh.shape[-1])))&#10;&#10;            result.append(oh)&#10;        return result&#10;&#10;    @staticmethod&#10;    def _indexed_ff_generator(buffer, advantages, num_mini_batch,&#10;                              prev_oh_flat_list=None):&#10;        &quot;&quot;&quot;&#10;        Feed-forward mini-batch generator that uses the **same random&#10;        indices** for both the replay-buffer data and the pre-computed&#10;        prev-adversary one-hot arrays.&#10;&#10;        Yields:&#10;            (sample_tuple, prev_oh_batch)&#10;            where prev_oh_batch is a list of np.ndarray [B, MAX_OH] or None.&#10;        &quot;&quot;&quot;&#10;        episode_length, n_rollout_threads = buffer.rewards.shape[0:2]&#10;        batch_size = n_rollout_threads * episode_length&#10;        mini_batch_size = batch_size // num_mini_batch&#10;&#10;        rand = torch.randperm(batch_size).numpy()&#10;        sampler = [rand[i * mini_batch_size:(i + 1) * mini_batch_size]&#10;                   for i in range(num_mini_batch)]&#10;&#10;        # Flatten buffer arrays (same as SeparatedReplayBuffer.feed_forward_generator)&#10;        share_obs = buffer.share_obs[:-1].reshape(-1, *buffer.share_obs.shape[2:])&#10;        obs = buffer.obs[:-1].reshape(-1, *buffer.obs.shape[2:])&#10;        rnn_states = buffer.rnn_states[:-1].reshape(-1, *buffer.rnn_states.shape[2:])&#10;        rnn_states_critic = buffer.rnn_states_critic[:-1].reshape(&#10;            -1, *buffer.rnn_states_critic.shape[2:])&#10;        actions = buffer.actions.reshape(-1, buffer.actions.shape[-1])&#10;        if buffer.available_actions is not None:&#10;            available_actions = buffer.available_actions[:-1].reshape(&#10;                -1, buffer.available_actions.shape[-1])&#10;        value_preds = buffer.value_preds[:-1].reshape(-1, 1)&#10;        returns = buffer.returns[:-1].reshape(-1, 1)&#10;        masks = buffer.masks[:-1].reshape(-1, 1)&#10;        active_masks = buffer.active_masks[:-1].reshape(-1, 1)&#10;        action_log_probs = buffer.action_log_probs.reshape(&#10;            -1, buffer.action_log_probs.shape[-1])&#10;        if buffer.factor is not None:&#10;            factor = buffer.factor.reshape(-1, buffer.factor.shape[-1])&#10;        adv_flat = advantages.reshape(-1, 1)&#10;&#10;        for indices in sampler:&#10;            share_obs_batch = share_obs[indices]&#10;            obs_batch = obs[indices]&#10;            rnn_states_batch = rnn_states[indices]&#10;            rnn_states_critic_batch = rnn_states_critic[indices]&#10;            actions_batch = actions[indices]&#10;            available_actions_batch = (available_actions[indices]&#10;                                       if buffer.available_actions is not None&#10;                                       else None)&#10;            value_preds_batch = value_preds[indices]&#10;            return_batch = returns[indices]&#10;            masks_batch = masks[indices]&#10;            active_masks_batch = active_masks[indices]&#10;            old_action_log_probs_batch = action_log_probs[indices]&#10;            adv_targ = adv_flat[indices]&#10;&#10;            if buffer.factor is None:&#10;                sample = (share_obs_batch, obs_batch, rnn_states_batch,&#10;                          rnn_states_critic_batch, actions_batch,&#10;                          value_preds_batch, return_batch, masks_batch,&#10;                          active_masks_batch, old_action_log_probs_batch,&#10;                          adv_targ, available_actions_batch)&#10;            else:&#10;                factor_batch = factor[indices]&#10;                sample = (share_obs_batch, obs_batch, rnn_states_batch,&#10;                          rnn_states_critic_batch, actions_batch,&#10;                          value_preds_batch, return_batch, masks_batch,&#10;                          active_masks_batch, old_action_log_probs_batch,&#10;                          adv_targ, available_actions_batch, factor_batch)&#10;&#10;            # Index prev_oh with same indices&#10;            prev_oh_batch = None&#10;            if prev_oh_flat_list is not None:&#10;                prev_oh_batch = [arr[indices] for arr in prev_oh_flat_list]&#10;&#10;            yield sample, prev_oh_batch&#10;&#10;    def prep_training(self):&#10;        self.policy.actor.train()&#10;        self.policy.critic.train()&#10;        if self.policy.guider is not None:&#10;            self.policy.guider.train()&#10;&#10;    def prep_rollout(self):&#10;        self.policy.actor.eval()&#10;        self.policy.critic.eval()&#10;        if self.policy.guider is not None:&#10;            self.policy.guider.eval()&#10;&#10;" />
              <option name="updatedContent" value="&quot;&quot;&quot;&#10;VAGGP_Trainer  -  PPO trainer with 4-term composite loss for VA-GGP&#10;====================================================================&#10;Drop-in replacement for R_MAPPO when training adversary agents with&#10;the Variational Autoregressive Guided Graph Policy architecture.&#10;&#10;Composite loss (per mini-batch):&#10;  L_total = L_distill  +  lambda_rl * L_rl  +  beta_recon * L_recon&#10;            + beta_kl * L_kl_bottleneck  -  entropy_coef * H(pi)&#10;&#10;where:&#10;  L_rl       = standard PPO-Clip surrogate&#10;  L_distill  = MAGPO double-clipped KL  (learner -&gt; guider)&#10;  L_recon    = MSE  decoder(z) vs. ground-truth prey relative positions&#10;  L_kl       = KL( q(z|obs) || N(0,I) )&#10;&quot;&quot;&quot;&#10;import numpy as np&#10;import torch&#10;import torch.nn as nn&#10;import torch.nn.functional as F&#10;&#10;from onpolicy.utils.util import get_gard_norm, huber_loss, mse_loss&#10;from onpolicy.utils.valuenorm import ValueNorm&#10;from onpolicy.algorithms.utils.util import check&#10;from onpolicy.algorithms.r_mappo.algorithm.adversary_vaggp_actor import (&#10;    VariationalBottleneck,&#10;    double_clip_distillation_loss,&#10;)&#10;&#10;&#10;class VAGGP_Trainer:&#10;    &quot;&quot;&quot;&#10;    PPO trainer extended with VA-GGP composite loss for adversary agents.&#10;&#10;    Identical to R_MAPPO except ppo_update() computes 4 loss terms&#10;    and jointly optimises learner + guider + critic.&#10;    &quot;&quot;&quot;&#10;&#10;    def __init__(self, args, policy, device=torch.device(&quot;cpu&quot;)):&#10;        self.device = device&#10;        self.tpdv = dict(dtype=torch.float32, device=device)&#10;        self.policy = policy&#10;&#10;        # ---- standard PPO hyper-parameters ----&#10;        self.clip_param = args.clip_param&#10;        self.ppo_epoch = args.ppo_epoch&#10;        self.num_mini_batch = args.num_mini_batch&#10;        self.data_chunk_length = args.data_chunk_length&#10;        self.value_loss_coef = args.value_loss_coef&#10;        self.entropy_coef = args.entropy_coef&#10;        self.max_grad_norm = args.max_grad_norm&#10;        self.huber_delta = args.huber_delta&#10;&#10;        self._use_recurrent_policy = args.use_recurrent_policy&#10;        self._use_naive_recurrent = args.use_naive_recurrent_policy&#10;        self._use_max_grad_norm = args.use_max_grad_norm&#10;        self._use_clipped_value_loss = args.use_clipped_value_loss&#10;        self._use_huber_loss = args.use_huber_loss&#10;        self._use_popart = args.use_popart&#10;        self._use_valuenorm = args.use_valuenorm&#10;        self._use_value_active_masks = args.use_value_active_masks&#10;        self._use_policy_active_masks = args.use_policy_active_masks&#10;&#10;        # ---- VA-GGP specific hyper-parameters ----&#10;        self.lambda_rl = getattr(args, 'vaggp_lambda_rl', 1.0)&#10;        self.beta_recon = getattr(args, 'vaggp_beta_recon', 0.1)&#10;        self.beta_kl = getattr(args, 'vaggp_beta_kl', 0.01)&#10;        self.delta_clip = getattr(args, 'vaggp_delta_clip', 1.5)&#10;&#10;        # ---- value normaliser ----&#10;        assert not (self._use_popart and self._use_valuenorm)&#10;        if self._use_popart:&#10;            self.value_normalizer = self.policy.critic.v_out&#10;        elif self._use_valuenorm:&#10;            self.value_normalizer = ValueNorm(1, device=self.device)&#10;        else:&#10;            self.value_normalizer = None&#10;&#10;    # ==================================================================&#10;    #  Value loss  (identical to R_MAPPO)&#10;    # ==================================================================&#10;    def cal_value_loss(self, values, value_preds_batch,&#10;                       return_batch, active_masks_batch):&#10;        value_pred_clipped = value_preds_batch + (&#10;            values - value_preds_batch&#10;        ).clamp(-self.clip_param, self.clip_param)&#10;&#10;        if self._use_popart or self._use_valuenorm:&#10;            self.value_normalizer.update(return_batch)&#10;            error_clipped = (&#10;                self.value_normalizer.normalize(return_batch) - value_pred_clipped)&#10;            error_original = (&#10;                self.value_normalizer.normalize(return_batch) - values)&#10;        else:&#10;            error_clipped = return_batch - value_pred_clipped&#10;            error_original = return_batch - values&#10;&#10;        if self._use_huber_loss:&#10;            vloss_c = huber_loss(error_clipped, self.huber_delta)&#10;            vloss_o = huber_loss(error_original, self.huber_delta)&#10;        else:&#10;            vloss_c = mse_loss(error_clipped)&#10;            vloss_o = mse_loss(error_original)&#10;&#10;        value_loss = (torch.max(vloss_o, vloss_c)&#10;                      if self._use_clipped_value_loss else vloss_o)&#10;&#10;        if self._use_value_active_masks:&#10;            value_loss = ((value_loss * active_masks_batch).sum()&#10;                          / active_masks_batch.sum())&#10;        else:&#10;            value_loss = value_loss.mean()&#10;        return value_loss&#10;&#10;    # ==================================================================&#10;    #  Composite PPO update  (the core of VA-GGP)&#10;    # ==================================================================&#10;    def ppo_update(self, sample, update_actor=True,&#10;                   prev_adv_actions_onehot=None):&#10;        &quot;&quot;&quot;&#10;        One gradient step on the 4-term composite loss.&#10;&#10;        Args:&#10;            sample:  standard PPO batch tuple from the buffer generator&#10;            update_actor:  whether to update the learner actor&#10;            prev_adv_actions_onehot:  list of [B, MAX_OH] tensors for the&#10;                autoregressive guider context, or None (first adversary&#10;                in the chain / guider disabled)&#10;&#10;        Returns:&#10;            (value_loss, critic_grad_norm, policy_loss, dist_entropy,&#10;             actor_grad_norm, imp_weights, diag_dict)&#10;        &quot;&quot;&quot;&#10;        # ---- unpack sample ----&#10;        if len(sample) == 12:&#10;            (share_obs_batch, obs_batch, rnn_states_batch,&#10;             rnn_states_critic_batch, actions_batch, value_preds_batch,&#10;             return_batch, masks_batch, active_masks_batch,&#10;             old_action_log_probs_batch, adv_targ,&#10;             available_actions_batch) = sample&#10;        else:&#10;            (share_obs_batch, obs_batch, rnn_states_batch,&#10;             rnn_states_critic_batch, actions_batch, value_preds_batch,&#10;             return_batch, masks_batch, active_masks_batch,&#10;             old_action_log_probs_batch, adv_targ,&#10;             available_actions_batch, _) = sample&#10;&#10;        old_action_log_probs_batch = check(old_action_log_probs_batch).to(**self.tpdv)&#10;        adv_targ = check(adv_targ).to(**self.tpdv)&#10;        value_preds_batch = check(value_preds_batch).to(**self.tpdv)&#10;        return_batch = check(return_batch).to(**self.tpdv)&#10;        active_masks_batch = check(active_masks_batch).to(**self.tpdv)&#10;&#10;        # ==============================================================&#10;        #  Forward pass:  learner evaluate_actions  (returns vaggp_info)&#10;        # ==============================================================&#10;        action_log_probs, dist_entropy, vaggp_info = \&#10;            self.policy.actor.evaluate_actions(&#10;                obs_batch, rnn_states_batch, actions_batch,&#10;                masks_batch, available_actions_batch, active_masks_batch)&#10;&#10;        values, _ = self.policy.critic(&#10;            check(share_obs_batch).to(**self.tpdv),&#10;            check(rnn_states_critic_batch).to(**self.tpdv),&#10;            check(masks_batch).to(**self.tpdv))&#10;&#10;        # ==============================================================&#10;        #  Loss 1:  Auxiliary RL  (standard PPO-Clip)&#10;        # ==============================================================&#10;        imp_weights = torch.exp(&#10;            action_log_probs - old_action_log_probs_batch)       # [B, 1]&#10;&#10;        surr1 = imp_weights * adv_targ&#10;        surr2 = torch.clamp(&#10;            imp_weights,&#10;            1.0 - self.clip_param,&#10;            1.0 + self.clip_param) * adv_targ&#10;&#10;        if self._use_policy_active_masks:&#10;            rl_loss = (&#10;                (-torch.sum(torch.min(surr1, surr2), dim=-1, keepdim=True)&#10;                 * active_masks_batch).sum() / active_masks_batch.sum())&#10;        else:&#10;            rl_loss = -torch.sum(&#10;                torch.min(surr1, surr2), dim=-1, keepdim=True).mean()&#10;&#10;        # ==============================================================&#10;        #  Loss 2:  MAGPO distillation  (double-clipped KL)&#10;        # ==============================================================&#10;        if (self.policy.guider is not None&#10;                and prev_adv_actions_onehot is not None):&#10;            # Guider produces reference probs (no gradient through guider)&#10;            with torch.no_grad():&#10;                guider_probs = self.policy.guider(&#10;                    share_obs_batch, prev_adv_actions_onehot,&#10;                    available_actions_batch)                       # [B, A]&#10;&#10;            # Learner full log-probs (need grad for distillation)&#10;            learner_logprobs = self.policy.actor.get_probs(&#10;                obs_batch, rnn_states_batch, masks_batch,&#10;                available_actions_batch).log()                    # [B, A]&#10;&#10;            distill_loss = double_clip_distillation_loss(&#10;                learner_logprobs, guider_probs, delta=self.delta_clip)&#10;        else:&#10;            distill_loss = torch.tensor(0.0, device=self.device)&#10;&#10;        # ==============================================================&#10;        #  Loss 3:  VGBSM reconstruction&#10;        #    decoder(z) should predict prey relative positions&#10;        # ==============================================================&#10;        decoded = vaggp_info['decoded']        # [B, num_good*2]&#10;        target = vaggp_info['target']          # [B, num_good*2]&#10;        recon_loss = F.mse_loss(decoded, target.detach())&#10;&#10;        # ==============================================================&#10;        #  Loss 4:  VGBSM KL bottleneck&#10;        #    KL( q(z|obs) || N(0,I) )&#10;        # ==============================================================&#10;        kl_bottleneck = VariationalBottleneck.kl_divergence(&#10;            vaggp_info['mu'], vaggp_info['logvar'])&#10;&#10;        # ==============================================================&#10;        #  Total actor loss  (4 terms + entropy bonus)&#10;        # ==============================================================&#10;        total_actor_loss = (&#10;            distill_loss&#10;            + self.lambda_rl * rl_loss&#10;            + self.beta_recon * recon_loss&#10;            + self.beta_kl * kl_bottleneck&#10;            - self.entropy_coef * dist_entropy&#10;        )&#10;&#10;        # ---- Backward + step  (learner) ----&#10;        self.policy.actor_optimizer.zero_grad()&#10;        if update_actor:&#10;            total_actor_loss.backward()&#10;        if self._use_max_grad_norm:&#10;            actor_grad_norm = nn.utils.clip_grad_norm_(&#10;                self.policy.actor.parameters(), self.max_grad_norm)&#10;        else:&#10;            actor_grad_norm = get_gard_norm(self.policy.actor.parameters())&#10;        self.policy.actor_optimizer.step()&#10;&#10;        # ---- Guider supervised update  (maximise log-prob of buffered actions) ----&#10;        if (self.policy.guider is not None&#10;                and prev_adv_actions_onehot is not None):&#10;            guider_lp, guider_ent = self.policy.guider.get_log_probs_and_entropy(&#10;                share_obs_batch, prev_adv_actions_onehot,&#10;                actions_batch, available_actions_batch, active_masks_batch)&#10;            guider_loss = -guider_lp.mean() - 0.01 * guider_ent&#10;&#10;            self.policy.guider_optimizer.zero_grad()&#10;            guider_loss.backward()&#10;            if self._use_max_grad_norm:&#10;                nn.utils.clip_grad_norm_(&#10;                    self.policy.guider.parameters(), self.max_grad_norm)&#10;            self.policy.guider_optimizer.step()&#10;&#10;        # ---- Critic update ----&#10;        value_loss = self.cal_value_loss(&#10;            values, value_preds_batch, return_batch, active_masks_batch)&#10;&#10;        self.policy.critic_optimizer.zero_grad()&#10;        (value_loss * self.value_loss_coef).backward()&#10;        if self._use_max_grad_norm:&#10;            critic_grad_norm = nn.utils.clip_grad_norm_(&#10;                self.policy.critic.parameters(), self.max_grad_norm)&#10;        else:&#10;            critic_grad_norm = get_gard_norm(self.policy.critic.parameters())&#10;        self.policy.critic_optimizer.step()&#10;&#10;        # ---- diagnostics dict ----&#10;        diag = dict(&#10;            recon_loss=recon_loss.item(),&#10;            kl_bottleneck=kl_bottleneck.item(),&#10;            distill_loss=(distill_loss.item()&#10;                          if torch.is_tensor(distill_loss)&#10;                          else distill_loss),&#10;        )&#10;        return (value_loss, critic_grad_norm, rl_loss, dist_entropy,&#10;                actor_grad_norm, imp_weights, diag)&#10;&#10;    # ==================================================================&#10;    #  Step 4: Policy back-tracking (representation sync)&#10;    # ==================================================================&#10;    def backtrack_sync(self):&#10;        &quot;&quot;&quot;&#10;        Copy compatible weights from the learner's graph-frontend&#10;        fusion layers into the guider's global encoder, keeping the&#10;        two representation spaces roughly aligned.&#10;        &quot;&quot;&quot;&#10;        if self.policy.guider is None:&#10;            return&#10;        src = self.policy.actor.graph.fusion.state_dict()&#10;        tgt = self.policy.guider.global_enc.state_dict()&#10;        for key in tgt:&#10;            if key in src and src[key].shape == tgt[key].shape:&#10;                tgt[key].copy_(src[key])&#10;        self.policy.guider.global_enc.load_state_dict(tgt)&#10;&#10;    # ==================================================================&#10;    #  Full training loop  (ppo_epoch x mini_batch)&#10;    # ==================================================================&#10;    def train(self, buffer, update_actor=True,&#10;              all_adv_buffers=None, adv_order_idx=0,&#10;              ar_prev_agents=None):&#10;        &quot;&quot;&quot;&#10;        Full training cycle.&#10;&#10;        Args:&#10;            buffer:           this agent's SeparatedReplayBuffer&#10;            update_actor:     whether to update the learner actor&#10;            all_adv_buffers:  list of all adversary SeparatedReplayBuffers&#10;                              (needed to build autoregressive context)&#10;            adv_order_idx:    this agent's position in the adversary ordering&#10;            ar_prev_agents:   (list[int] | None) shuffled list of adversary&#10;                              agent-IDs that precede this agent in the&#10;                              current AR ordering.  When provided, overrides&#10;                              the fixed 0..adv_order_idx-1 ordering.&#10;        &quot;&quot;&quot;&#10;        # ---- compute advantages ----&#10;        if self._use_popart or self._use_valuenorm:&#10;            advantages = (buffer.returns[:-1]&#10;                          - self.value_normalizer.denormalize(&#10;                              buffer.value_preds[:-1]))&#10;        else:&#10;            advantages = buffer.returns[:-1] - buffer.value_preds[:-1]&#10;        adv_copy = advantages.copy()&#10;        adv_copy[buffer.active_masks[:-1] == 0.0] = np.nan&#10;        mean_adv = np.nanmean(adv_copy)&#10;        std_adv = np.nanstd(adv_copy)&#10;        advantages = (advantages - mean_adv) / (std_adv + 1e-5)&#10;&#10;        # ---- pre-compute flattened prev-agent one-hot arrays ----&#10;        # Each is np.ndarray (T*N, MAX_OH) matching the buffer's flat ordering&#10;        prev_oh_flat_list = None&#10;        if all_adv_buffers is not None:&#10;            # AR Order Shuffling: use the shuffled prev-agent list when provided&#10;            if ar_prev_agents is not None and len(ar_prev_agents) &gt; 0:&#10;                prev_oh_flat_list = self._build_prev_onehot_from_list(&#10;                    all_adv_buffers, ar_prev_agents)&#10;            elif ar_prev_agents is None and adv_order_idx &gt; 0:&#10;                # Fallback to fixed order for backward compatibility&#10;                prev_oh_flat_list = self._build_prev_onehot(&#10;                    all_adv_buffers, adv_order_idx)&#10;&#10;        # ---- training loop ----&#10;        train_info = dict(&#10;            value_loss=0, policy_loss=0, dist_entropy=0,&#10;            actor_grad_norm=0, critic_grad_norm=0, ratio=0,&#10;            recon_loss=0, kl_bottleneck=0, distill_loss=0)&#10;&#10;        for _ in range(self.ppo_epoch):&#10;            # Use custom indexed generator so prev_oh aligns with sample&#10;            gen = self._indexed_ff_generator(&#10;                buffer, advantages, self.num_mini_batch,&#10;                prev_oh_flat_list)&#10;&#10;            for sample, prev_oh_batch in gen:&#10;                ctx = None&#10;                if prev_oh_batch is not None:&#10;                    ctx = [torch.from_numpy(t) for t in prev_oh_batch]&#10;&#10;                result = self.ppo_update(&#10;                    sample, update_actor,&#10;                    prev_adv_actions_onehot=ctx)&#10;&#10;                (value_loss, critic_grad_norm, policy_loss,&#10;                 dist_entropy, actor_grad_norm, imp_weights, diag) = result&#10;&#10;                train_info['value_loss'] += value_loss.item()&#10;                train_info['policy_loss'] += policy_loss.item()&#10;                train_info['dist_entropy'] += dist_entropy.item()&#10;                train_info['actor_grad_norm'] += actor_grad_norm&#10;                train_info['critic_grad_norm'] += critic_grad_norm&#10;                train_info['ratio'] += imp_weights.mean()&#10;                train_info['recon_loss'] += diag['recon_loss']&#10;                train_info['kl_bottleneck'] += diag['kl_bottleneck']&#10;                train_info['distill_loss'] += diag['distill_loss']&#10;&#10;        num_updates = self.ppo_epoch * self.num_mini_batch&#10;        for k in train_info:&#10;            train_info[k] /= num_updates&#10;&#10;        # Step 4: back-tracking sync after each training cycle&#10;        self.backtrack_sync()&#10;        return train_info&#10;&#10;    # ==================================================================&#10;    #  Helpers&#10;    # ==================================================================&#10;    @staticmethod&#10;    def _build_prev_onehot(all_adv_buffers, adv_order_idx):&#10;        &quot;&quot;&quot;&#10;        Build one-hot action arrays for adversary agents 0..adv_order_idx-1.&#10;&#10;        Returns a list of np.ndarray [T*N, MAX_OH=9] (one per prev agent),&#10;        where T=episode_length, N=n_rollout_threads.&#10;&#10;        NOTE: buf.actions has shape (episode_length, N, act_dim) — NO +1,&#10;              so we do NOT strip the last row.&#10;        &quot;&quot;&quot;&#10;        MAX_OH = 5   # all adversaries are Discrete(5), no leader comm&#10;        result = []&#10;        for prev_idx in range(adv_order_idx):&#10;            buf = all_adv_buffers[prev_idx]&#10;            # buf.actions shape: (episode_length, n_threads, act_dim)&#10;            acts = buf.actions                          # (T, N, act_dim)&#10;            flat = acts.reshape(-1, acts.shape[-1])     # (T*N, act_dim)&#10;            act_dim = flat.shape[-1]&#10;&#10;            if act_dim == 1:&#10;                # Discrete(5): single integer action -&gt; one-hot(5)&#10;                # (all adversaries are equal — no leader, all silent)&#10;                oh = np.zeros((flat.shape[0], 5), dtype=np.float32)&#10;                idx = flat[:, 0].astype(int).clip(0, 4)&#10;                oh[np.arange(len(idx)), idx] = 1.0&#10;            else:&#10;                oh = np.zeros((flat.shape[0], MAX_OH), dtype=np.float32)&#10;&#10;            # Pad to MAX_OH if needed&#10;            if oh.shape[-1] &lt; MAX_OH:&#10;                oh = np.pad(oh, ((0, 0), (0, MAX_OH - oh.shape[-1])))&#10;&#10;            result.append(oh)&#10;        return result&#10;&#10;    @staticmethod&#10;    def _build_prev_onehot_from_list(all_adv_buffers, prev_agent_ids):&#10;        &quot;&quot;&quot;&#10;        Build one-hot action arrays for a *shuffled* list of preceding agents.&#10;&#10;        Unlike _build_prev_onehot (which always uses agents 0..idx-1),&#10;        this accepts an arbitrary ordered list of agent IDs, enabling&#10;        Autoregressive Order Shuffling to break the fixed decision order.&#10;&#10;        Args:&#10;            all_adv_buffers:  list of all adversary SeparatedReplayBuffers&#10;            prev_agent_ids:   list[int] of adversary agent IDs in the&#10;                              shuffled AR order that precede the current agent.&#10;        Returns:&#10;            list of np.ndarray [T*N, 5] (one per preceding agent), or None.&#10;        &quot;&quot;&quot;&#10;        if not prev_agent_ids:&#10;            return None&#10;        MAX_OH = 5   # all adversaries are Discrete(5), no leader comm&#10;        result = []&#10;        for prev_idx in prev_agent_ids:&#10;            buf = all_adv_buffers[prev_idx]&#10;            acts = buf.actions                          # (T, N, act_dim)&#10;            flat = acts.reshape(-1, acts.shape[-1])     # (T*N, act_dim)&#10;            act_dim = flat.shape[-1]&#10;&#10;            if act_dim == 1:&#10;                oh = np.zeros((flat.shape[0], 5), dtype=np.float32)&#10;                idx = flat[:, 0].astype(int).clip(0, 4)&#10;                oh[np.arange(len(idx)), idx] = 1.0&#10;            else:&#10;                oh = np.zeros((flat.shape[0], MAX_OH), dtype=np.float32)&#10;&#10;            if oh.shape[-1] &lt; MAX_OH:&#10;                oh = np.pad(oh, ((0, 0), (0, MAX_OH - oh.shape[-1])))&#10;&#10;            result.append(oh)&#10;        return result&#10;&#10;    @staticmethod&#10;    def _indexed_ff_generator(buffer, advantages, num_mini_batch,&#10;                              prev_oh_flat_list=None):&#10;        &quot;&quot;&quot;&#10;        Feed-forward mini-batch generator that uses the **same random&#10;        indices** for both the replay-buffer data and the pre-computed&#10;        prev-adversary one-hot arrays.&#10;&#10;        Yields:&#10;            (sample_tuple, prev_oh_batch)&#10;            where prev_oh_batch is a list of np.ndarray [B, MAX_OH] or None.&#10;        &quot;&quot;&quot;&#10;        episode_length, n_rollout_threads = buffer.rewards.shape[0:2]&#10;        batch_size = n_rollout_threads * episode_length&#10;        mini_batch_size = batch_size // num_mini_batch&#10;&#10;        rand = torch.randperm(batch_size).numpy()&#10;        sampler = [rand[i * mini_batch_size:(i + 1) * mini_batch_size]&#10;                   for i in range(num_mini_batch)]&#10;&#10;        # Flatten buffer arrays (same as SeparatedReplayBuffer.feed_forward_generator)&#10;        share_obs = buffer.share_obs[:-1].reshape(-1, *buffer.share_obs.shape[2:])&#10;        obs = buffer.obs[:-1].reshape(-1, *buffer.obs.shape[2:])&#10;        rnn_states = buffer.rnn_states[:-1].reshape(-1, *buffer.rnn_states.shape[2:])&#10;        rnn_states_critic = buffer.rnn_states_critic[:-1].reshape(&#10;            -1, *buffer.rnn_states_critic.shape[2:])&#10;        actions = buffer.actions.reshape(-1, buffer.actions.shape[-1])&#10;        if buffer.available_actions is not None:&#10;            available_actions = buffer.available_actions[:-1].reshape(&#10;                -1, buffer.available_actions.shape[-1])&#10;        value_preds = buffer.value_preds[:-1].reshape(-1, 1)&#10;        returns = buffer.returns[:-1].reshape(-1, 1)&#10;        masks = buffer.masks[:-1].reshape(-1, 1)&#10;        active_masks = buffer.active_masks[:-1].reshape(-1, 1)&#10;        action_log_probs = buffer.action_log_probs.reshape(&#10;            -1, buffer.action_log_probs.shape[-1])&#10;        if buffer.factor is not None:&#10;            factor = buffer.factor.reshape(-1, buffer.factor.shape[-1])&#10;        adv_flat = advantages.reshape(-1, 1)&#10;&#10;        for indices in sampler:&#10;            share_obs_batch = share_obs[indices]&#10;            obs_batch = obs[indices]&#10;            rnn_states_batch = rnn_states[indices]&#10;            rnn_states_critic_batch = rnn_states_critic[indices]&#10;            actions_batch = actions[indices]&#10;            available_actions_batch = (available_actions[indices]&#10;                                       if buffer.available_actions is not None&#10;                                       else None)&#10;            value_preds_batch = value_preds[indices]&#10;            return_batch = returns[indices]&#10;            masks_batch = masks[indices]&#10;            active_masks_batch = active_masks[indices]&#10;            old_action_log_probs_batch = action_log_probs[indices]&#10;            adv_targ = adv_flat[indices]&#10;&#10;            if buffer.factor is None:&#10;                sample = (share_obs_batch, obs_batch, rnn_states_batch,&#10;                          rnn_states_critic_batch, actions_batch,&#10;                          value_preds_batch, return_batch, masks_batch,&#10;                          active_masks_batch, old_action_log_probs_batch,&#10;                          adv_targ, available_actions_batch)&#10;            else:&#10;                factor_batch = factor[indices]&#10;                sample = (share_obs_batch, obs_batch, rnn_states_batch,&#10;                          rnn_states_critic_batch, actions_batch,&#10;                          value_preds_batch, return_batch, masks_batch,&#10;                          active_masks_batch, old_action_log_probs_batch,&#10;                          adv_targ, available_actions_batch, factor_batch)&#10;&#10;            # Index prev_oh with same indices&#10;            prev_oh_batch = None&#10;            if prev_oh_flat_list is not None:&#10;                prev_oh_batch = [arr[indices] for arr in prev_oh_flat_list]&#10;&#10;            yield sample, prev_oh_batch&#10;&#10;    def prep_training(self):&#10;        self.policy.actor.train()&#10;        self.policy.critic.train()&#10;        if self.policy.guider is not None:&#10;            self.policy.guider.train()&#10;&#10;    def prep_rollout(self):&#10;        self.policy.actor.eval()&#10;        self.policy.critic.eval()&#10;        if self.policy.guider is not None:&#10;            self.policy.guider.eval()&#10;&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>